# 星河宠记 · 部署配置（2026-08-22）

> **规范（用户要求，以后所有项目都要）**：每个项目的部署配置统一记录在各自仓库的 `05-部署配置/` 目录；
> **密钥/密码/连接串的值永不写入本文件**（安全红线），只记录键名与获取方式，值见服务器 `.env`。
> 最后更新：2026-09-09（Seedance 2.0 mini 选型实测 + 回忆录视频生产链路打通）

---

## 一、服务器

| 项 | 值 |
| --- | --- |
| IP | `49.232.203.85`（腾讯云） |
| 账号 | `root`（密码/密钥见用户，不落盘） |
| 系统 | Ubuntu 22.04.5 LTS |
| Node | v20.20.2 |
| 用途 | 星河宠记 + 爱需说出口 共用一台 |

## 二、应用

| 项 | 值 |
| --- | --- |
| 部署路径 | `/opt/xinghuanhai`（server 在 `/opt/xinghuanhai/server`） |
| **PM2 进程名** | **`xinghuanhai-server`**（⚠️ 运营文档此前误写 xinghechongji-server，已修正） |
| 端口 | 3000 |
| API 域名 | `api.xinghuanhai.com`（PUBLIC_BASE_URL） |
| 日志 | `/opt/xinghuanhai/logs/`（PM2 out/err） |

## 三、数据库

| 项 | 值 |
| --- | --- |
| 库名 | `xinghuanhai`（PostgreSQL） |
| 连接 | `.env` 的 `DATABASE_URL`（值不落盘） |
| 迁移机制 | `server/migrations/*.sql` 按文件名升序执行（`npm run migrate`） |
| 坑记录 | 跨用户建表 owner：`agent_memories` 曾属 postgres（其他会话创建），已 `ALTER OWNER TO xinghuanhai`；新表/新列用幂等语句（IF NOT EXISTS） |

## 四、环境变量清单（键名，值见服务器 .env）

| 键 | 用途 | 获取方式 |
| --- | --- | --- |
| PORT / DATABASE_URL / JWT_SECRET | 基础 | 服务器 .env |
| AI_API_KEY / AI_BASE_URL / AI_MODEL | DeepSeek 对话（旧语义） | DeepSeek 控制台 |
| ARK_API_KEY / ARK_BASE_URL / ARK_MODEL | 火山方舟（新语义，待配） | 火山方舟控制台 |
| BAILIAN_API_KEY / BAILIAN_BASE_URL / BAILIAN_VISION_MODEL / BAILIAN_ASR_MODEL | 百炼视觉/语音 | 阿里云百炼 |
| SEEDREAM_API_KEY | 火山方舟生图/视频 | 火山方舟 |
| SEEDANCE_API_KEY / SEEDANCE_MODEL | Seedance 视频 | 火山方舟 |
| **DOUBAO_SPEECH_API_KEY**（+VOICE/RESOURCE_ID/BASE_URL） | 回忆录旁白（豆包语音） | 火山"语音技术"控制台 |
| **QUALITY_CHECK_API_KEY**（+BASE_URL/MODEL） | 回忆录视觉质检（DeepSeek vision-exp） | DeepSeek 控制台 |
| WECHAT_APPID / WECHAT_SECRET / WECHAT_PAY_* | 微信/支付 | 微信公众平台/商户平台 |
| MESHY_API_KEY | 3D（未启用） | Meshy |
| MODERATE_API_KEY | 内容审核 | 火山内容安全 |
| PUSHPLUS_TOKEN | 反馈推送 | PushPlus |
| **ADMIN_TOKEN** | 知识图谱审核后台（管理接口 x-admin-token，未配置时管理接口一律 403） | 自生成高强度随机串（`openssl rand -hex 32`），仅存服务器 .env |
| UPLOAD_DIR / PUBLIC_BASE_URL / LOG_LEVEL | 基础设施 | — |

## 五、AI 依赖（服务器已装/已配）

| 依赖 | 状态 | 说明 |
| --- | --- | --- |
| ffmpeg 4.4.2 | ✅ 已装 | 回忆录拼接/字幕/转场必需 |
| fonts-noto-cjk | ✅ 已装 | 中文字幕（fc-cache 已重建） |
| 豆包语音（seed-tts-2.0） | ✅ key 已配 | 回忆录旁白（SSE 流式） |
| DeepSeek 视觉（vision-exp） | ✅ 可用 | 质检/体检识别（需配 QUALITY_CHECK_API_KEY） |
| ~~edge-tts~~ | 已卸载 | 改云端豆包语音（合规） |

## 六、部署步骤

```bash
# 1. 更新代码（git pull 或上传部署包）
# 2. 安装依赖
cd /opt/xinghuanhai/server && npm install
# 3. 执行数据库迁移（幂等；022_knowledge_graph.sql 自动建 knowledge_graphs / knowledge_feedback 表）
npm run migrate
# 4. 重启
pm2 restart xinghuanhai-server
# 5. 验证
curl https://api.xinghuanhai.com/api/health
curl https://api.xinghuanhai.com/api/knowledge/latest   # 图谱下发（空表自动用种子播种）
# 6. 管理后台（浏览器）: https://api.xinghuanhai.com/admin （输入 ADMIN_TOKEN）
```

> ⚠️ 迁移遇"must be owner of table"：用 `sudo -u postgres psql -d <库名>` 执行，并 `ALTER TABLE ... OWNER TO xinghuanhai`。
> ⚠️ 知识图谱上线前必须配 `ADMIN_TOKEN`（管理接口 fail-closed）；种子 `server/src/data/medicalGraphSeed.json` 是前端图谱快照，**改 `medicalGraph.ts` 需同步重新导出**（DB 播种后服务端以 DB 为权威）。

## 七、监控

- 服务器监控（每 5 分钟 cron + 微信告警，`/srv/ops/monitor.sh`）：星河宠记 API(:3000) / 爱需说出口 API(:3001) / PostgreSQL / 磁盘(>85%) / 内存(<200MB) / SSL 证书(<14天)；告警去重（恢复前只发一次）。
- **数据库备份（2026-08-23 修复 P0 缺口）**：每日 3 点 cron，`/srv/ops/backup-xinghuanhai.sh` 备份 **xinghuanhai 库**（保留 15 份，失败微信告警）；爱需说出口库由 `/srv/ops/backup-aixu.sh` 备份（保留 15 份）。
- **异地备份（2026-08-23）**：本地电脑每日 07:00 计划任务 `XHH-Backup-Pull` 运行 `05-部署配置/backup-local-pull.js`（免密 SSH，本机 `~/.ssh/id_ed25519`，公钥已装服务器 authorized_keys），从服务器拉取最新备份到本地 `E:\Backups\xinghuanhai\`（保留 30 份）。**恢复演练仍未做（待补）**。
- **AI 余额告警（2026-08-23，2026-08-24 增强）**：每日 9:00 cron `/srv/ops/check-balance.sh` 查 DeepSeek 官方余额（AI_API_KEY），不可用或 <¥20 微信告警（阈值默认 20，可经 /etc/aixu-monitor.env 的 DS_BALANCE_THRESHOLD 覆盖）；**2026-08-24 新增** 9:05 cron `/srv/ops/check-balance-cloud.sh` 覆盖阿里云百炼 + 火山方舟/Seedream 余额（**需先在 .env 配 ALIYUN_AK/ALIYUN_SK、VOLC_AK/VOLC_SK 账号 AccessKey，API Key 查不了余额（已实测）**，未配置时记录 SKIP 不告警）。
- **支付对账（2026-08-23）**：每日 9:30 cron `/srv/ops/reconcile-payments.sh`，统计昨日创建/支付成功/超48h未支付/数据不一致，异常微信告警（当前实测全零无异常）。
- **运维脚本版本管理（2026-08-23）**：`05-部署配置/monitor/` 为唯一事实源（git 管理），服务器 `/srv/ops/` 为生产副本；改动流程 = 仓库改 → 上传 → 冒烟。
- **待补（P1）**：备份**恢复演练**已做过一次（建议定期复演）；**阿里云/火山引擎 AK/SK 未配**——用户提供 `ALIYUN_AK/ALIYUN_SK`、`VOLC_AK/VOLC_SK` 填服务器 .env 后，云平台余额告警即自动生效（脚本已部署）。

## 八、部署记录

| 日期 | 操作 | 结果 |
| --- | --- | --- |
| 2026-08-22 | migration 018（health_reports 表） | ✅ 创建成功（owner xinghuanhai） |
| 2026-08-22 | migration 019（agent_memories 加 tags/level） | ✅ 执行成功（先移交 owner） |
| 2026-08-22 | 冒烟验证（表/列存在） | ✅ |
| 2026-08-22 | 安装 ffmpeg / 中文字体 / 豆包语音 | ✅ |
| 2026-08-22 | 卸载 edge-tts | ✅（改云端豆包） |
| 2026-08-22 | 时光页升级部署：迁移 020（pet_moments 加 happened_at）+ 021（改 DATE 类型），owner 移交 xinghuanhai，重启 PM2 | ✅ 端到端验证（补记日期/排序/AI 润色/删除/归属校验） |
| 2026-08-22 | db.ts date 类型解析器（返回 YYYY-MM-DD 字符串，修时区差一天） | ✅ 验证 happened_at 返回纯日期 |
| 2026-08-23 | 修复 analytics 埋点 429/500：①全局限流 skip 豁免 /api/analytics/*（originalUrl 判断，埋点走自身 120/分钟限流）；②建 `analytics_events` 表（对齐真实 schema：users.id uuid、pet_profiles.id text），重启 PM2 | ✅ 公网连发 65 次全 200、数据写入验证、测试数据已清理；服务端 44 文件 792 测试全绿 |
| 2026-08-23 | 知识图谱全流程合入（Phase1 规则外置+置信度 → Phase2 会员AI深度分析+记忆召回 → Phase3 热更新+纠错+审核后台；恢复事件记忆；聊天 check_symptom 消费权威图谱） | ✅ **已部署**（详见下行） |
| 2026-08-23 | 知识图谱上线：上传 17 个变更文件 → 迁移 022（knowledge_graphs/knowledge_feedback 建表，owner xinghuanhai）→ ADMIN_TOKEN 写入 .env（openssl 随机，值未落盘文档）→ 重启 PM2（进程 60 次重启后稳定在线）→ 冒烟 | ✅ 全绿：health 200；/api/knowledge/latest 返回种子 v2026-08-22.1；/admin/admin.html 200；管理接口无 Token 403（fail-closed）；日志无 error；备份 src.bak.20260823040411-knowledge |
| 2026-08-23 | 知识图谱部署补充说明 | ⚠️ 旧迁移 003/004/005/008/009/011/012/013/014/017 仍因 owner 失败（既有问题，不影响新表）；**root 密码已暴露于一次聊天记录，建议尽快改密**；ADMIN_TOKEN 值在服务器 .env，查看方式：`ssh root@49.232.203.85 "grep '^ADMIN_TOKEN=' /opt/xinghuanhai/server/.env"` |
| 2026-08-23 | Agent 对话成本日志（AI 算账）：上传 agentService.ts（意图/工具链/token/耗时记录，try/finally 兜底客户端断开）+ 迁移 023 建 agent_conversation_logs 表（CHECK 约束 + 双索引）→ 重启 PM2 | ✅ 冒烟全绿：health 200；表结构验证（10 列 + 索引 + CHECK）；日志静默失败不阻塞对话；用户消息正文不落库 |
| 2026-08-23 | 修复喂养建议 500（缺表）：①发现迁移 012/013 原版外键引用 `pets(id)`（表不存在），生产实际为 `pet_profiles(id)`（text）——已修正迁移脚本为对齐生产 schema；②用 postgres 超级用户执行建表 `pet_feeding_records` / `pet_chronic_records` + 索引，owner 移交 xinghuanhai；③重启 PM2 | ✅ 冒烟全绿：GET/POST feeding-records 200、GET chronic 200、外键指向 pet_profiles/users 正确、应用用户可查询；测试写入数据已清理；服务健康 |
| 2026-08-23 | ⚠️ 遗留（未处理，非本次报障范围）：迁移 014（suggestion_records）、017（invite_referrals）表名与生产实际命名不一致（生产已有 hospital_referrals/invite_codes/referral_records），若对应功能 500 需单独对齐 | 待确认 |
| 2026-08-23 | 监控备份体系补齐：①星河宠记库每日备份（backup-xinghuanhai.sh，3点 cron，此前 P0 缺口：只有 qinglv 库有备份）②异地备份到本地（XHH-Backup-Pull 计划任务 7 点拉取到 E:\Backups，checksum 一致）③DeepSeek 余额告警（9 点，<¥10，当前 ¥73.21）④支付对账（9:30，异常告警，实测全零）⑤运维脚本入库 05-部署配置/monitor/ | ✅ 全部实测通过；**恢复演练**：备份还原临时库成功（56 表 + users16/pet_profiles15/agent_memories3/knowledge_graphs1），临时库已清理 |
| 2026-08-23 | **修复生产 429（点击慢性病等多处报错，与缺表 500 是两个问题）**：根因=nginx 反代未开 trust proxy（req.ip 恒 127.0.0.1）+ 全局限流跑在认证前（userId 恒空）→ 所有用户共享一个 60/分钟桶，突发即集体 429。修复：`app.set('trust proxy',1)` + 新增 `resolveUserId` 中间件（限流前解析 JWT、不拒绝请求）→ 按真实用户独立限流。验证：用户A/用户B 各突发 60 次均 0 个 429（PASS） | ✅ 已部署（d1d12e1），847/847 测试通过 |
| 2026-08-23 | 知识图谱/监控配置写入项目记忆（AGENTS.md） | ✅ 已更新 |
| 2026-08-23 | 修复宠物档案切换宠物 429（限流误伤正常操作）：全局限流 60→120 次/分钟（切换宠物时档案页/时光页多模块并发加载约 6-8 请求/次，60 次/分钟过严触发误伤）→ 上传 rateLimit.ts → 重启 PM2 | ✅ 冒烟：130 请求 = 120×200 + 10×429（新阈值精确生效）；服务端 52 文件 848 测试全绿（顺带修复 knowledge.test.ts 动态版本断言 bug） |
| 2026-08-23 | **兑换码系统上线 + 不忘 VIP 发放**：①迁移 024 建 redeem_codes 表；②`POST /api/redeem` 用户兑换发/续会员（active 顺延，CAS 防重复兑换）；③管理端 `POST/GET /api/admin/redeem-codes`（ADMIN_TOKEN，adminAuth 抽为共享中间件）；④小程序会员页新增兑换码输入入口（e1a207e，待微信发版）；⑤**走完整流程给不忘（dfae8e5b…可乐/布丁主人）发放 100 年会员**：生成码 XHH-MKXK-4FNQ-R3HL → 签 JWT → 兑换成功（expires_at 2126-07-30，memberships 表验证 tier=member/active） | ✅ 已部署（d1f8c18），855/855 测试通过 |
| 2026-08-23 | ⚠️ **管理后台访问约定**：**必须用域名 `https://api.xinghuanhai.com/admin`**，**禁用 IP `https://49.232.203.85/admin`**——共用服务器上 qxl（爱需说出口）是 443 默认站点，IP 直连会命中它并返回"接口不存在"；且证书按域名签发，IP 不匹配。管理页改 JS/CSS 后记得递增 `admin.html` 里的 `?v=` 版本号强制刷新缓存（本次 v=3） | ✅ 已修复 /admin/ 404（0489247）+ 缓存版本号（911a44c） |
| 2026-08-23 | **修复管理页 JS/CSS 域名下 404**：nginx `location ~* \.(js\|css)$` 正则把 /admin/admin.js 当磁盘静态文件（/var/www/xinghuanhai 下不存在 → 404），而 admin.html 走回后端正常——导致页面骨架能显示但脚本样式全挂（"点生成没反应"的深层根因）。修复：nginx 加 `location ^~ /admin/ { proxy_pass :3000 }`（^~ 优先级高于正则），管理页资源全部走 Express 单一来源；配置已入库 `05-部署配置/monitor/nginx-xinghuanhai.conf`，备份 xinghuanhai.bak-20260823 | ✅ nginx -t 通过 → reload → 域名下 admin.js/css?v=3 与 /admin/ 全部 200；v=3 版本号配合根治缓存 |
| 2026-08-23 | **喂养建议 + 慢性病追踪接入 AI（会员专属，前后端）**：用户要求"不接 AI 怎么做喂食建议"→ 参照症状 AI 模式新增 feedingAiService/chronicAiService（注入宠物档案+喂养记录/慢病记录+记忆召回→LLM→输出安全检测→降级兜底）+ 路由 POST /feeding-records/ai-analysis、/chronic/ai-analysis（会员强制+zod+归属校验）+ 前端 AI 卡片（"生成 AI 建议"按钮）。踩坑：guardCheckOutput 的 LLM 检测对"就医提醒"误判且不稳定（同文本 medical/care 结果相反）→ care 场景改用确定性规则检测（处方药名/确诊式结论） | ✅ 已部署：服务端 55 文件 869 测试全绿（新增 feedingAi 8 + chronicAi 6）；生产冒烟喂养 AI 返回 6 条真实建议（unsafe=false，服务端权威档案覆盖前端传参）；慢性病 AI 空记录正确返回引导文案；schema 校验 400 生效；前端 tsc 0 错误 + 2333 测试通过 + dist 已重建 |
| 2026-08-23 | **慢性病"发现"机制上线（慢病风险扫描，会员专属）**：用户问"怎么发现慢性病"→ 新增 POST /chronic/scan-risk：L2 规则预警（近 90 天打卡确定性规则：高风险频率/体重异常趋势/持续异常，ruleBasedSignals 导出可单测）+ L3 AI 疑似识别（LLM 识别慢病模式，仅"疑似/建议排查"，guard care 场景检测）；前端慢性病页顶部加"慢病风险扫描"卡片（信号列表+AI 洞察+扫描按钮） | ✅ 已部署：服务端 57 文件 882 测试全绿（新增 chronicRisk 路由 6 + 规则单测 7）；生产冒烟：打卡不足时正确返回"数据不足+建议规律打卡"（不误报）、AI 给出疑似触发条件+免责声明；前端 tsc 0 错误 + 2333 测试通过 + dist 已重建 |
| 2026-08-24 | **Agent 技能补全（3 个新工具，已部署）**：用户盘点 Agent 技能后要求补缺口 → 新增 get_chronic_advice（复用 chronicAiService 慢病管理建议）、get_feeding_advice（复用 feedingAiService 喂养建议）、scan_chronic_risk（复用 chronicRiskService 风险扫描）三个 AI 工具；toolRegistry 定义 15→18、agentTools 注册 15→18、agentService prompt 增加"慢病管理/喂养建议/风险扫描"路由示例；工具内部直接复用服务（Agent 场景无需会员校验，聊天已登录） | ✅ 已部署：服务端 882 测试全绿；生产日志确认"18 个工具已注册完成"；待办=周报/回忆录/形象定制等重流程工具（涉及 familyId/流程跳转，复杂度高，留后续） |
| 2026-08-24 | **webp→PNG 兼容性修复（体验版图片不显示/上传失败）**：①根因=微信安卓真机对 webp（尤其 VP8X+ALPH 带透明）解码兼容性差，模拟器正常真机空白（登录主图/品牌 logo/预选头像/家庭页头像全受影响）；②本地 23 张 webp 全转 PNG（预选头像 160px+256 色调色板，分包 1.80MB 保持限内）；③服务器 home-style 20 张头像补传 PNG（256px 全彩，备份 home-style.bak-20260824024517）；④**修复 nginx 静态资源 404 bug**：`location ~* \.(jpg|png|...)$` 只加缓存头无回退，/uploads/** 实际在 Express 托管磁盘（/opt/xinghuanhai/server/uploads）→ 公网 404（webp 不在正则内反而正常，解释了此前"webp 能加载、png 404"的怪象）→ 加 `try_files $uri @backend` 回退；⑤前端 homeStyleAvatars URL 改 .png + 测试断言同步；⑥隐私声明 errno 112（chooseImage 未在《用户隐私保护指引》声明）需用户在微信后台配置，已确认配置完成 | ✅ nginx -t + reload 通过；公网 3 张 png 头像 200；前端 2361 测试全绿（原 avatarPresets 既有失败已修正断言） |
| 2026-08-24 | **Agent 18 工具全量冒烟测试 + 修复 6 个缺陷（已部署）**：写脚本直接调 executeTool 遍历 18 工具（真实 VIP 用户可乐）。首轮发现 6 个问题并修复：①get_vaccine_calendar 查 pet_vaccines（表不存在）→ 改 pet_vaccinations + 字段对齐（无 vaccine_name，用 type/category）；②search_breed_info 查 pet_breeds（表不存在）→ 改为查 pet_profiles 品种 + 引导话术；③search_hospital 查 pet_hospitals（表不存在）→ 改为就医指引话术（紧急/非紧急）；④record_health_checkin 中文枚举直接写 smallint 列（类型错误）→ 加中文→数值映射（1-5）；⑤risk_level 用了 caution/normal（违反 CHECK 约束 low/medium/high/emergency）→ 改 high/low；⑥record_feeding 给 bigint 主键传 UUID → 省略 id 让序列自增。另发现测试脚本需 import agentTools 才触发注册（生产由 agentRouter import，无此问题） | ✅ 已部署：重测 18/18 全部 PASS（含 3 个 AI 工具真实 LLM 调用、写入类测试后清理）；服务端 882 测试全绿；前端无改动 |
| 2026-08-24 | **时光轴/登录会员迁移/趋势周报批次部署（cfa75e4 + 1f1f34e）**：上传 10 个变更文件（timeline 路由/仓库、db.ts、rateLimit.ts、schemas、慢性病/喂养 AI 服务等）→ 迁移 012/013/020/021 全部 ✓（**此前手工建表的 012/013 修正脚本正式跑通**，累计 14 成功 / 8 个既有 owner 失败）→ 备份 src.bak.-batch → PM2 重启 | ✅ 冒烟全绿：日志无 error/TypeError；进程 online；health 200；knowledge/latest 200；checkins 无 token 401（鉴权生效） |
| 2026-08-24 | **AI 余额告警增强**：①DeepSeek 阈值 ¥10→¥20（check-balance.sh 改 DS_BALANCE_THRESHOLD 可覆盖）；②实测确认百炼/火山 **API Key 查不了余额**（火山返回 InvalidAuthorization、阿里需 AK/SK 签名）→ 新增 check-balance-cloud.sh（阿里云 BSS HMAC-SHA1 + 火山费用中心 HMAC-SHA256 完整签名），9:05 cron，未配 AK/SK 时记录 SKIP；用户配 .env 的 ALIYUN_AK/SK、VOLC_AK/SK 即生效 | ✅ 语法 OK、未配置行为正确（SKIP 落日志不告警）、cron 已挂；DeepSeek 余额实测 ¥43.05（正常） |
| 2026-08-24 | **多成员共同养宠部署（M1-M5 后端）**：打包上传 src+migrations（备份 src.bak.20260824-family）→ 迁移 025（pet_family_users 建表+回填 owner）/026（pet_family_invites 邀请码表）全部 ✓（累计 16 成功 / 8 个既有 owner 失败不变）→ PM2 重启 | ✅ 冒烟全绿：health 200；pet_family_users 表结构+索引+回填 2 行；创建家庭自动 owner；邀请码生成（6 位）/凭码加入/成员列表 2 人；越权拦截（未入家庭宠物打卡 404、非 owner 移除成员 403）；家庭成员共管打卡 201 + 查看历史 200（pet 维度含成员记录）；周报成员可读（无周报 404 非 403）；测试数据已清理 |
| 2026-08-24 | **全家福生图提示词修复部署（commit 32f0c7e，无数据库迁移）**：上传 4 文件（新增 petPrompt.ts 提示词公共模块 + familyPhotoService/avatarService/image2DService 修改——宠物名字彻底退出生图提示词（防「烧鸡」被画成鸡）、明确数量与物种、参考图一致性+主体锁定、全量生图服务统一接入《宠物回忆录-提示词库.md》§0.6/§四/§六 规范）→ 备份 3 个旧文件到 /opt/xinghuanhai/src.bak.promptfix-* → PM2 重启 | ✅ 冒烟全绿：health 200；重启后日志无 error/TypeError；生产实测 buildPrompt（模拟四只猫含「烧鸡」）→ 名字/鸡类词不进提示词（false/false）、数量 4 只/皮克斯风格/参考图一致性/主体锁定均生效（true×4）；本地服务端 931 测试全绿；回滚=恢复 src.bak.promptfix-* 后重启 |
| 2026-08-24 | **数据修复：删除错误的"可乐-布丁"配偶关系**：用户反馈家族图谱"本该指向父母的手势指向了布丁"→ 查库定位根因=pet_relationships 表存在一条错误 mate 关系（93b95cf5…，可乐↔布丁，2026-08-23 00:35 创建，疑为当时测试添加关系误选"伴侣"），导致可乐/布丁图谱"配偶"层互相错误显示（二人实为同窝兄妹，父母同为烧鸭 7c0468be）。处置=DELETE 该关系（DELETE 1）→ 复查 pet_relationships 涉及可乐/布丁 0 条、血缘 2 条（烧鸭→可乐、烧鸭→布丁）保持正确；无代码改动、无重启 | ✅ 数据复查通过：错误 mate 已删净（0 残留）、血缘不受影响；用户前端重新编译/刷新即可看到配偶层清空 |
| 2026-08-24 | **全家福生图参考图分级部署（commit e53ca17，无数据库迁移）**：上传 2 文件（familyPhotoService.ts 新增 isBrandPresetUrl 品牌默认头像判定 + collectMemberPhotos 分级过滤（真实形象=真实照片/AI 生成形象，品牌 home-style/预设图不算）+ generateFamilyPhoto 校验 MEMBER_NO_REAL_IMAGE；familyPhotos.ts 失败响应透传 code/missingMembers）→ 备份 2 旧文件到 /opt/xinghuanhai/src.bak.photograde-* → PM2 重启。前端配套（dashboard 引导弹窗跳转形象定制页并自动切换缺形象宠物、预设形象卡去品种名、api/store 错误码透传、savePhoto photoType 契约修复）已构建，需用户微信开发者工具重新编译 | ✅ 冒烟全绿：health 200；日志无 error；文件校验（MEMBER_NO_REAL_IMAGE×2、missingMembers×1）；生产实测 isBrandPresetUrl（品牌 URL→true、真实照片 URL→false）+ buildPrompt 回归（不写名字/数量正确）；本地前后端全量 938+2371 测试全绿；回滚=恢复 src.bak.photograde-* 后重启 |
| 2026-08-24 | **形象定制"文字描述生成"补输入框并接入生图（commit 6966e99，无数据库迁移）**：用户反馈"文字生成没有输入文字的地方、提示生成了看不到在哪"→ 修复：①服务端 generatePetImageOptions 新增 description 参数（用户描述清洗去换行/截断 100 字后拼进各画风提示词）+ routes/avatar.ts generate-options 接收并透传（生成记录含描述）；②前端形象定制页文字生成 Tab 新增描述输入框（Textarea，placeholder 引导 + "不写名字"提示），生成按钮传描述；③生成成功后 pageScrollTo 滚动到候选列表（"选择你喜欢的形象"在面板底部，此前用户看不到）；④免费用户引导文案补"照片生成 Tab 可上传照片作头像"；⑤补测试（后端 avatarService.test.ts 3 用例 + 前端 avatarService.test.ts 3 用例） | ✅ 已部署：上传 avatarService.ts + avatar.ts（备份 src.bak.avatardesc-*）→ PM2 重启 → 冒烟 health 200、进程 online、文件校验通过；本地后端 941 测试全绿 + 前端 2379 测试全绿 + build:weapp 成功；回滚=恢复 src.bak.avatardesc-* 后重启 |
| 2026-08-24 | **形象定制页重构 + 形象库（commit 4c19fe0，含迁移 028）**：用户需求=删除冗余"风格切换/表情系统"展示卡；文字生成改为手动选画风+表情+描述（参考提示词模板指引）生成 1 张；新增形象库按风格/表情分类保存多次生成的形象。实现=①迁移 028 新建 pet_avatar_library（⚠️ user_id 用 **UUID** 对齐 users.id，审查 P0 修正；UNIQUE(pet_id,image_url) 防重复收藏）；②服务端 generatePetImageOptions 支持 styleKey（单画风生成 1 张）+ expression（EXPRESSION_PROMPTS 12 表情中文提示词）；generate-options 接收 styleKey/expression（白名单）+ **文字/照片配额口径拆分**（文字 style=options-*-text-*，照片=options-*-photo，countMonthlyOptionsByUser 排除 -text-%）；新增 /library 三接口（POST 保存/GET 查询/DELETE 删除，宠物归属+style 白名单+imageUrl http(s) 校验+expression 白名单）；③前端：删冗余卡、文字 Tab 改造（描述+参考模板+画风单选+表情单选+生成1张）、结果区单张（存入形象库/设为当前/重新生成）、形象库卡（画风/表情筛选、点击设为当前、删除二次确认）、照片 5 候选补"存入形象库"；**getAvatarLibrary snake→camel 映射**（审查 P0 修正，否则 imageUrl undefined）；清理死样式/死代码/过时文案；④测试：服务端 avatarService.test.ts 6 例 + avatar.library.test.ts 10 例、前端 avatarService.test.ts 27 例；双 Agent 审查（2 P0 已修 + P1×3 已修 + P2 部分） | ✅ 已部署：迁移 028 建表成功（user_id uuid、索引、owner 移交）→ 备份 src.bak.avatarlib-* → 替换 3 文件（repository/service/routes）→ PM2 重启 → 冒烟 health 200、日志无 error、表行数 0（空表正常）；本地后端 954 测试全绿 + 前端 2385 测试全绿 + build:weapp 成功；回滚=恢复 src.bak.avatarlib-* 后重启（表可留着） |
| 2026-08-24 | **画风扩展至 15 种 + 参考模板公式化（commit e8c41b8，无数据库迁移）**：用户反馈"画风应该不止这一点、提示词参考模板太简约"→ ①服务端 AVATAR_STYLE_OPTIONS 5 种扩展为 **15 种**（新增吉卜力/皮克斯3D/像素/水墨/油画/赛博朋克/极简北欧/低多边形/线稿/暗黑奇幻 10 种，每风格猫/狗描述融合《宠物回忆录-提示词库.md》§6/§7.1 关键词；VHS/故障艺术对头像效果难保证暂不收）；新增 DEFAULT_STYLE_KEYS（默认 5 画风池，照片生成批量候选仍 5 张不放大成本）；②前端画风 chips 15 种 + 参考模板改为**提示词库式公式**（主体/外貌/表情/画风/氛围五字段说明 + 实时完整示例 + 「填入外貌+表情」一键按钮 + 每画风光影氛围词 GEN_STYLE_ATMOS） | ✅ 已部署：上传 avatarService.ts（备份 src.bak.styles15-*）→ PM2 重启 → 冒烟 health 200、日志无 error；本地后端 954 + 前端 2385 测试全绿 + build:weapp 成功；回滚=恢复 src.bak.styles15-* 后重启 |
| 2026-08-24 | **照片自动提取详细外貌进生图提示词（commit 5758bcf，无数据库迁移）**：用户反馈"提示词描写不详细，怎么用橘色虎斑草草描述"（提示词库 §0.9 要求每一个细节都描写）→ ①服务端新增 extractPetAppearance（复用 visionService 的 DeepSeek 视觉模型，prompt 要求输出毛色/花纹/体型/脸型/眼睛/鼻子/胡须/特殊标记，清洗去换行/截断 100 字）；generate-options 集成：用户未写描述且宠物有真实照片（avatar_photo_url）时自动提取外貌拼进提示词（仅"读照片描述"，不传参考图、不触发照片生成配额，文字生成口径不变；提取失败降级档案描述）；②前端：输入框 placeholder 改为详细示范（橘色虎斑英短…白下巴胸毛）、公式区"外貌"字段多维引导、模板示例 fallback 详细化（毛色/花纹/体型/脸型/眼睛/鼻头/标记）、提示"有真实照片留空自动提取"；③测试：extractPetAppearance 3 例 + 路由自动提取 4 例（有照片/用户描述优先/无照片/提取失败降级） | ✅ 已部署：上传 avatarService.ts + avatar.ts（备份 src.bak.appearance-*）→ PM2 重启 → 冒烟 health 200、日志无 error；本地后端 961 + 前端 2385 测试全绿 + build:weapp 成功；回滚=恢复 src.bak.appearance-* 后重启 |
| 2026-08-25 | **修复 AI 润色间歇性 503（thinking 吃光 token 预算，无数据库迁移）**：用户报 `/api/timeline/ai-polish` 503 → 生产日志实锤同一用户 02:27 成功后 02:28 连续两次 503（4s+ 耗时）；服务器实测复现=deepseek-v4-flash 思考默认开启，润色路由 max_tokens=400 且未关思考，思考吃光预算时 finish=length、正文零 token → 空串 → 路由 503「AI 润色失败」（同参数连发 6 次出现 1 次 finish=length、reasoning 512 字符独占预算）。修复=routes/timeline.ts 润色调用加 `thinking: 'disabled'`（对齐 visionService 等新服务口径）+ max_tokens 400→800；补回归锁测试（断言 chat 必须带 thinking disabled）。生产 API 实测 thinking disabled 六连发：reasoning 全为 0、finish 全 stop、正文全非空 | ✅ 已部署：备份 routes/timeline.ts 到 /opt/xinghuanhai/src.bak.aipolish-20260825024113 → scp 上传 → PM2 重启 online → 冒烟 health 200、ai-polish 无 token 401（路由存活）、重启后日志无 error；本地 tsc 0 + timeline 20/20；回滚=恢复 src.bak.aipolish-* 后重启 |
| 2026-08-25 | **补配视觉模型密钥 QUALITY_CHECK_API_KEY（激活全部看图能力，仅 .env 变更无代码迁移）**：排查发现生产/本地 .env 均未配置 QUALITY_CHECK_API_KEY → visionService（DeepSeek deepseek-v4-flash-vision-exp）自上线起一直走"未配置降级"，AI 写描述永远 503「AI 视觉能力未配置」、照片自动提取外貌静默降级档案描述、回忆录视频质检降级。处置=服务器上从主 AI_API_KEY 复制同值写入 QUALITY_CHECK_API_KEY（两者同为 DeepSeek 官方 api.deepseek.com 不存在混配）；交叉验证 T1 主key+vision模型=200 证明该账号有 vision-exp 权限；首次 printf 追加损坏（残留行致 source 后为空）→ 改 scp 脚本方式重写（清理脏行+tr 剥 CR 引号+三重校验 FILE_LINE_OK/SOURCE_OK/HEALTH=200）；真实识图冒烟 http=200 返回图片内容 | ✅ 已生效：PM2 重启 health 200；识图冒烟 200+内容正确；备份 .env.bak.viskey-* 两份；回滚=恢复 .env 备份后 restart。⚠️ 教训：ssh 内嵌 printf 追加 .env 会因引号/换行层损坏内容，改用 scp 脚本文件执行 |
| 2026-08-25 | **四批次合并部署：全家福22场景+水印B方案 / 形象一套两张(迁移030) / 名字→外貌翻译层 / 品种知识库(迁移031)**：迁移 029（family_photos.scene）+030（pet_avatar_library.view_type + pet_profiles.avatar_multiview_url）+031（breed_knowledge 表，owner=xinghuanhai）全部 ✓；上传 20 文件（index.ts 挂载 /api/breeds、routes/{avatar,familyPhotos,pets,breeds}.ts、services/{petPrompt,familyPhotoService,image2DService,avatarService,imageBadge}.ts、repositories/{avatarRepository,petRepository,breedRepository}.ts、schemas、breedSeed.json、assets/ai-badge.png、package.json）→ npm install 装 jimp@0.22.12（新增88包）→ PM2 重启 | ⚠️✅ 首启崩溃循环：imageBadge.ts 用了 `__dirname`，生产 ESM 下不存在（本地 vitest 走 CJS 转换测不出）→ 改 `fileURLToPath(import.meta.url)` 推导（同 breedRepository 既有模式）单文件热修后恢复。最终冒烟全绿：health 公网 200；GET /api/breeds/knowledge 200（version 2026-08-25.1，110品种带来源标注）；background-swap 与全家福 POST 无 token 均 401（鉴权存活）；进程 online 稳定。回滚=恢复 src.bak.batch0825-20260825041703 后重启（迁移列可留着，均向后兼容）。⚠️ 教训沉淀：①凡新增用 `__dirname`/`__filename` 的模块必须 ESM 兼容写法，vitest 全绿≠生产可启动——建议部署前先 `node --experimental-strip-types` 类真 ESM 冒烟或 grep `__dirname` 把关；②PowerShell 管道会损坏 tar 二进制流，二进制打包传输必须"落盘→scp→远端解压"；③并行会话活跃期部署需冻结清单逐文件核对，避免把半成品带上生产 |
| 2026-08-25 | **品种知识库双 Agent 审查加固部署（commit 39667f3+6a1beec，无新迁移——迁移031与种子数据已在上一批上线且 DB 播种数据经核验为修复后版本）**：上传 2 文件（routes/breeds.ts 校验加固：id/name≤50、sources 元素≤100、条目≤300、**id 重复拒绝**、补 aliases 数组+weightRange.min/max 渲染必需字段校验（防坏库穿透打崩 checkin/edit/add/trends 多页）；repositories/breedRepository.ts getLatestBreeds 补 console.error 排障日志）→ 备份到 /opt/xinghuanhai/src.bak.breedharden-20260825 → PM2 重启。前端配套（getActiveBreeds 切换层/syncBreedKnowledge 三级降级/6 页面消费替换/校验同口径闭合）已构建进 dist，需用户微信开发者工具重新编译 | ✅ 部署前核验生产行情：GET /api/breeds/knowledge 200、110 条、三译名生效（沙特尔猫/尼比龙猫/索科凯猫，旧名留 aliases）、金毛 6 源含 UC Davis VGL/OMIA/ASPCA 条件源——确认并行会话 04:17 批次携带的已是修复后种子，DB 播种数据无需处理；本次仅补代码加固。冒烟全绿：health 200；knowledge 200 条数/version 不变；PUT /api/admin/breeds 无 token 403（adminAuth fail-closed）；进程 online 稳定（114s 无重启）；重启后日志无 error（04:21 __dirname 崩溃为上批历史已修）；本地 server tsc 0 + 全量 1017 passed、miniapp tsc 0 + 全量 2407 passed + build EXIT 0；回滚=恢复 src.bak.breedharden-20260825 两文件后重启 |
| 2026-08-25 | **关闭3D模型生成 + AI页打卡弹窗化批次（commit d477797 服务端 / 28ee6eb+88ad982 纯前端）**：①服务端仅 1 文件——routes/avatar.ts（MEMBER_3D_MONTHLY_LIMIT 3→0，配额归零返回 403 code=FEATURE_DISABLED「3D 模型功能暂未开放」与正常用尽 QUOTA_EXCEEDED 区分）；②打卡弹窗卡片化（CheckinPopup 组件/首页六入口接线/checkinService 正常档评级修复）与登录守卫收口均为纯小程序前端，已构建进 dist 无需服务器部署、无数据库迁移 | ✅ 已部署：备份 src/routes/avatar.ts 到 src.bak.20260825-deploy3d → scp 上传 → grep 校验 MEMBER_3D_MONTHLY_LIMIT=0 与 closedByConfig 分支在位 → PM2 重启 online → 冒烟全绿：health 200、POST generate-3d 无 token 401（路由加载正常）、error 日志空；回滚=恢复 src.bak.20260825-deploy3d 后重启。⚠️ 前端生效需用户微信开发者工具重新编译/上传体验版 |
| 2026-09-01 | **回忆录 Seedance 视频提示词升级部署（commit 9552224，无数据库迁移）**：上传 7 文件（新增 services/memoirPhotoAnalysis.ts 逐张照片视觉摘要 + 修改 memoirProcessor/memoirScriptService/promptTemplates/seedanceAdapter/memoirScript(schema)/videoGenerationService——宠物名字彻底退出 anchors/identity_anchor/seedance_prompt（含历史脚本重试入口）、十段 prompt 对齐 Seedance 2.5 官方工程公式、静图微动作/单运镜/五锁、首帧 role=first_frame、screenDirection 默认保持首帧原始朝向、schema prompt 上限 500→1500）→ 备份 6 旧文件到 /opt/xinghuanhai/src.bak.memoirprompt-20260901191744 → scp 上传 → 文件与关键标记 grep 校验（analyzeMemoirPhotos×2/×1、sanitizeMemoirScriptPrompts×3、first_frame×1、ANATOMY×2）→ PM2 重启（restarts 105） | ✅ 冒烟全绿：health 200（两次）；回忆录 status/create 无 token 401（路由加载正常、鉴权生效，证明新文件 import 链完整）；进程 online 稳定；重启后 error 日志空；本地定向 53/53 + build 通过；回滚=恢复 src.bak.memoirprompt-20260901191744 后重启 |
| 2026-09-01 | **回忆录记忆锚定防编造 + 时光线素材源部署（commit fcc92a2/c791fb3，无数据库迁移）**：上传 3 文件（memoirScriptService.ts 系统提示词新增【记忆锚定规则】——叙事只能以记忆摘要/用户自述/照片视觉摘要为事实来源、三类全空触发【无记忆约束】禁止虚构具体事件、删"合理构思"邀请语；memoryService.ts 新增 getPetMomentsSummary 读时光线 pet_moments 回忆文本（jsonb_typeof 防标量、北京时区归日、120 字截断）；memoirProcessor.ts 记忆引擎+时光线双源并入 memorySummary）→ 备份 3 旧文件到 /opt/xinghuanhai/src.bak.memanchor-20260908195359 → scp 上传 → grep 校验（记忆锚定规则×1、getPetMomentsSummary×1/×2、Asia.Shanghai×2）→ PM2 重启（restarts 106） | ✅ 冒烟全绿：health 200；回忆录路由无 token 401（import 链完整）；进程 online；error 日志空；本地定向 35/35（分镜 24 + 时光线 6 + 截断时区新增）+ build 通过 + 全量 1032/1033（唯一失败为既有会员日期用例）；子代理审查通过（P0=0/P1=0，P2×2 已修）；回滚=恢复 src.bak.memanchor-20260908195359 后重启。⚠️ 前端配套（两页回忆标签 chips + source_text bug 修复，commit b1662a1）需微信开发者工具重新编译发版 |
| 2026-09-08 | **R1 上线前 P0 清单（立项书 v0.2 §八）代码完成，待部署**——P0-1 会员权益重构（促销价仅保留年费 88，月/季恢复常规 29.9/79.9；照片流 3套→2套降本 33.6→22.4 元/次；照片月配额 3→1；会员页展示价对齐服务端）；P0-2 剧本确认闸门（迁移 032 幂等 3 列+局部索引；awaiting_confirmation 旗标暂停待确认，确认后回队生成，放弃零成本；状态接口透传 script；前端确认卡）；P0-3 AI 隐式标识（aigcMetadata.ts：PNG tEXt AIGC + MP4 moov/udta AIGC，零依赖手写 CRC32/ISOBMFF，失败不阻断；imageBadge 落盘前打标、processor 成片打标）；P0-4 AI 对话合规降温（§七.4 五项验收全部完成，留档 01-产品文档/立项/资料/AI对话合规自查-2026-09-08.md） | ✅ **已部署（2026-09-09 03:30，与审查修复批次 610a3b7 合并上线）**：部署前核验发现**审查修复批次此前未上生产**（生产 rateLimit 无 skipInTest 实锤）→ 合并部署：①.env 三项经 scp 脚本写入 WECHAT_PAY_MOCK=false + ALLOWED_ORIGINS（www/xinghuanhai/api 三域名白名单）+ MODERATE_API_KEY 留 TODO 注释（**用户必办：补火山内容安全 key**，缺失时启动醒目告警+视频审核 fail-open）；②迁移 032 首跑暴露表名错误（memoir_tasks→实际 pet_memoir_records，以 repo tableName 为准）→ 修正后重跑 3×ALTER+CREATE INDEX 全成功；③tar 打包 35 文件（审查批 29 + R1 去重：config/index/rateLimit/13 路由/3 仓库/14 服务/schemas/ecosystem）→ 备份 /opt/xinghuanhai/src.bak.r1-20260909 → 解压覆盖 → PM2 重启；④冒烟全绿：api-health 200、进程 online 稳定（restart 107 无崩溃循环）、memoir-confirm/membership 无 token 401（新路由加载+鉴权生效）、error 日志仅预期告警（MODERATE_API_KEY fail-open 提醒）、生产文件标记抽查 pause_gate/stamp/disclaimer/guard/aigc_png 全命中、032 三列 psql 确认存在；回滚=恢复 /opt/xinghuanhai/src.bak.r1-20260909 后重启（032 列可留着，向后兼容）。⚠️ 教训重申：ssh 内嵌复杂命令 PowerShell 引号层必炸——env/冒烟全部走 scp 脚本文件执行 |
| 2026-09-09 | **回忆录视频模型选型实测 + Seedance 生产链路打通（无代码提交，仅 .env 配置）**：发现生产默认模型 doubao-seedance-1-5-pro-251215 已 Retiring 不可开通（方舟 models API 全量列表实证）且生产 pet_memoir_records 表 0 行+日志 0 调用=视频链从未真实执行（隐藏欠账）→ 用户开通 doubao-seedance-2-0-mini-260615 → 本地方舟 API 同图同提示词 A/B 实测：720p 与豆包 App 免费档同质量且推镜更忠实（停在胸口以上 vs 豆包超纲满屏特写）、480p 身份动作全保持但特写变软→用户拍板 480p 太糊选 720p → 生产+本地 .env 各加 SEEDANCE_MODEL=doubao-seedance-2-0-mini-260615（adapter 环境变量覆盖零代码改动；产品代码 resolution 本就写死 720p）→ PM2 重启 108 → 冒烟 health 200/日志干净/online 稳定。实测成本：2.0 mini 720p≈4元/8s镜，75 秒纪念 Vlog 全片约 36-40 元（vs 原估 1.5 Pro 纸面价 20 元）——⚠️ 对现有定价偏贵，待定价决策（涨价/混档/高清增值档）。交付物：E:\Codex\2026-09-09\回忆录视频模型实测\outputs\（豆包App版+API 720p+API 480p+三版同刻对比图）。⚠️ 教训新增：PowerShell Get-Content+Set-Content 全文件重写必毁 UTF-8 中文（GBK 误读），全文件编辑一律 edit 工具；git checkout <commit> -- <file> 是字节级救援正道 |
| 2026-09-09 04:10 | **迁移校准收口（审查⏳5「上线前必办」，用户「继续」授权）**：生产 schema 全景比对 63 表 vs 本地 32 迁移——①**修复两个生产 P0 缺口**：(a) 迁移 009 从未生效（payment_orders 缺 product_type/product_metadata/transaction_id 三列）→ paymentOrderRepository 引用这些列，**回忆录付费下单必 500**（主验证付费点，尚未被真实用户踩中）→ 幂等补跑 4×ALTER+2 索引成功；(b) 迁移 014 原脚本双重错误（pet_id UUID REFERENCES pets(id)——生产无 pets 表、pet_profiles.id 实为 text，与 012/013 同类）→ pet_suggestion_records 从未建成，前端情绪干预/照护计划卡 4 处消费 500 → 修正 014（pet_id TEXT REFERENCES pet_profiles(id)）补跑建表+3 索引成功；②死迁移归档（头部注释「已被生产等价实现取代，不可重放」）：003（生产 avatar_generations，代码已对齐）/004（wardrobe 无代码引用）/005（生产 audit_log 单数，代码已对齐）/011（user_phones 无引用）/017（代码用 referral_records）；③其余 22 个迁移均已在生产生效（012/013/020/021/022-032 历史批次+032 今日）；④校准结论：迁移与生产 schema 已全量对齐，「不可重放」风险收口 | ✅ 生产验证：009 三列 psql 确认在位、pet_suggestion_records 建成（pet_id text）、/api/pets/x/suggestions 401（路由+鉴权存活）、进程 online；回滚=无需（均为纯增量表/列/索引，向后兼容） |
