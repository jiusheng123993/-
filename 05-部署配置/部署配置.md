# 星河宠记 · 部署配置（2026-08-22）

> **规范（用户要求，以后所有项目都要）**：每个项目的部署配置统一记录在各自仓库的 `05-部署配置/` 目录；
> **密钥/密码/连接串的值永不写入本文件**（安全红线），只记录键名与获取方式，值见服务器 `.env`。
> 最后更新：2026-08-22（迁移 018/019 部署完成）

---

## 一、服务器

| 项 | 值 |
| --- | --- |
| IP | `49.232.203.85`（腾讯云） |
| 账号 | `root`（密码/密钥见用户，不落盘） |
| 系统 | Ubuntu 22.04.5 LTS |
| Node | v20.20.2 |
| 用途 | 星河宠记 + 爱需说出口 共用一台 |

## 二、应用

| 项 | 值 |
| --- | --- |
| 部署路径 | `/opt/xinghuanhai`（server 在 `/opt/xinghuanhai/server`） |
| **PM2 进程名** | **`xinghuanhai-server`**（⚠️ 运营文档此前误写 xinghechongji-server，已修正） |
| 端口 | 3000 |
| API 域名 | `api.xinghuanhai.com`（PUBLIC_BASE_URL） |
| 日志 | `/opt/xinghuanhai/logs/`（PM2 out/err） |

## 三、数据库

| 项 | 值 |
| --- | --- |
| 库名 | `xinghuanhai`（PostgreSQL） |
| 连接 | `.env` 的 `DATABASE_URL`（值不落盘） |
| 迁移机制 | `server/migrations/*.sql` 按文件名升序执行（`npm run migrate`） |
| 坑记录 | 跨用户建表 owner：`agent_memories` 曾属 postgres（其他会话创建），已 `ALTER OWNER TO xinghuanhai`；新表/新列用幂等语句（IF NOT EXISTS） |

## 四、环境变量清单（键名，值见服务器 .env）

| 键 | 用途 | 获取方式 |
| --- | --- | --- |
| PORT / DATABASE_URL / JWT_SECRET | 基础 | 服务器 .env |
| AI_API_KEY / AI_BASE_URL / AI_MODEL | DeepSeek 对话（旧语义） | DeepSeek 控制台 |
| ARK_API_KEY / ARK_BASE_URL / ARK_MODEL | 火山方舟（新语义，待配） | 火山方舟控制台 |
| BAILIAN_API_KEY / BAILIAN_BASE_URL / BAILIAN_VISION_MODEL / BAILIAN_ASR_MODEL | 百炼视觉/语音 | 阿里云百炼 |
| SEEDREAM_API_KEY | 火山方舟生图/视频 | 火山方舟 |
| SEEDANCE_API_KEY / SEEDANCE_MODEL | Seedance 视频 | 火山方舟 |
| **DOUBAO_SPEECH_API_KEY**（+VOICE/RESOURCE_ID/BASE_URL） | 回忆录旁白（豆包语音） | 火山"语音技术"控制台 |
| **QUALITY_CHECK_API_KEY**（+BASE_URL/MODEL） | 回忆录视觉质检（DeepSeek vision-exp） | DeepSeek 控制台 |
| WECHAT_APPID / WECHAT_SECRET / WECHAT_PAY_* | 微信/支付 | 微信公众平台/商户平台 |
| MESHY_API_KEY | 3D（未启用） | Meshy |
| MODERATE_API_KEY | 内容审核 | 火山内容安全 |
| PUSHPLUS_TOKEN | 反馈推送 | PushPlus |
| **ADMIN_TOKEN** | 知识图谱审核后台（管理接口 x-admin-token，未配置时管理接口一律 403） | 自生成高强度随机串（`openssl rand -hex 32`），仅存服务器 .env |
| UPLOAD_DIR / PUBLIC_BASE_URL / LOG_LEVEL | 基础设施 | — |

## 五、AI 依赖（服务器已装/已配）

| 依赖 | 状态 | 说明 |
| --- | --- | --- |
| ffmpeg 4.4.2 | ✅ 已装 | 回忆录拼接/字幕/转场必需 |
| fonts-noto-cjk | ✅ 已装 | 中文字幕（fc-cache 已重建） |
| 豆包语音（seed-tts-2.0） | ✅ key 已配 | 回忆录旁白（SSE 流式） |
| DeepSeek 视觉（vision-exp） | ✅ 可用 | 质检/体检识别（需配 QUALITY_CHECK_API_KEY） |
| ~~edge-tts~~ | 已卸载 | 改云端豆包语音（合规） |

## 六、部署步骤

```bash
# 1. 更新代码（git pull 或上传部署包）
# 2. 安装依赖
cd /opt/xinghuanhai/server && npm install
# 3. 执行数据库迁移（幂等；022_knowledge_graph.sql 自动建 knowledge_graphs / knowledge_feedback 表）
npm run migrate
# 4. 重启
pm2 restart xinghuanhai-server
# 5. 验证
curl https://api.xinghuanhai.com/api/health
curl https://api.xinghuanhai.com/api/knowledge/latest   # 图谱下发（空表自动用种子播种）
# 6. 管理后台（浏览器）: https://api.xinghuanhai.com/admin （输入 ADMIN_TOKEN）
```

> ⚠️ 迁移遇"must be owner of table"：用 `sudo -u postgres psql -d <库名>` 执行，并 `ALTER TABLE ... OWNER TO xinghuanhai`。
> ⚠️ 知识图谱上线前必须配 `ADMIN_TOKEN`（管理接口 fail-closed）；种子 `server/src/data/medicalGraphSeed.json` 是前端图谱快照，**改 `medicalGraph.ts` 需同步重新导出**（DB 播种后服务端以 DB 为权威）。

## 七、监控

- 服务器监控（每 5 分钟 cron + 微信告警，`/srv/ops/monitor.sh`）：星河宠记 API(:3000) / 爱需说出口 API(:3001) / PostgreSQL / 磁盘(>85%) / 内存(<200MB) / SSL 证书(<14天)；告警去重（恢复前只发一次）。
- **数据库备份（2026-08-23 修复 P0 缺口）**：每日 3 点 cron，`/srv/ops/backup-xinghuanhai.sh` 备份 **xinghuanhai 库**（保留 15 份，失败微信告警）；爱需说出口库由 `/srv/ops/backup-aixu.sh` 备份（保留 15 份）。
- **异地备份（2026-08-23）**：本地电脑每日 07:00 计划任务 `XHH-Backup-Pull` 运行 `05-部署配置/backup-local-pull.js`（免密 SSH，本机 `~/.ssh/id_ed25519`，公钥已装服务器 authorized_keys），从服务器拉取最新备份到本地 `E:\Backups\xinghuanhai\`（保留 30 份）。**恢复演练仍未做（待补）**。
- **AI 余额告警（2026-08-23）**：每日 9:00 cron `/srv/ops/check-balance.sh`，查 DeepSeek 官方余额（AI_API_KEY），不可用或 <¥10 微信告警。ARK/百炼/Seedream 余额需控制台 AK/SK，本期未覆盖（待补）。
- **支付对账（2026-08-23）**：每日 9:30 cron `/srv/ops/reconcile-payments.sh`，统计昨日创建/支付成功/超48h未支付/数据不一致，异常微信告警（当前实测全零无异常）。
- **运维脚本版本管理（2026-08-23）**：`05-部署配置/monitor/` 为唯一事实源（git 管理），服务器 `/srv/ops/` 为生产副本；改动流程 = 仓库改 → 上传 → 冒烟。
- **待补（P1）**：备份**恢复演练**（异地副本已就位，需演练恢复流程）；ARK/百炼/Seedream 余额告警（需控制台密钥）。

## 八、部署记录

| 日期 | 操作 | 结果 |
| --- | --- | --- |
| 2026-08-22 | migration 018（health_reports 表） | ✅ 创建成功（owner xinghuanhai） |
| 2026-08-22 | migration 019（agent_memories 加 tags/level） | ✅ 执行成功（先移交 owner） |
| 2026-08-22 | 冒烟验证（表/列存在） | ✅ |
| 2026-08-22 | 安装 ffmpeg / 中文字体 / 豆包语音 | ✅ |
| 2026-08-22 | 卸载 edge-tts | ✅（改云端豆包） |
| 2026-08-22 | 时光页升级部署：迁移 020（pet_moments 加 happened_at）+ 021（改 DATE 类型），owner 移交 xinghuanhai，重启 PM2 | ✅ 端到端验证（补记日期/排序/AI 润色/删除/归属校验） |
| 2026-08-22 | db.ts date 类型解析器（返回 YYYY-MM-DD 字符串，修时区差一天） | ✅ 验证 happened_at 返回纯日期 |
| 2026-08-23 | 修复 analytics 埋点 429/500：①全局限流 skip 豁免 /api/analytics/*（originalUrl 判断，埋点走自身 120/分钟限流）；②建 `analytics_events` 表（对齐真实 schema：users.id uuid、pet_profiles.id text），重启 PM2 | ✅ 公网连发 65 次全 200、数据写入验证、测试数据已清理；服务端 44 文件 792 测试全绿 |
| 2026-08-23 | 知识图谱全流程合入（Phase1 规则外置+置信度 → Phase2 会员AI深度分析+记忆召回 → Phase3 热更新+纠错+审核后台；恢复事件记忆；聊天 check_symptom 消费权威图谱） | ✅ **已部署**（详见下行） |
| 2026-08-23 | 知识图谱上线：上传 17 个变更文件 → 迁移 022（knowledge_graphs/knowledge_feedback 建表，owner xinghuanhai）→ ADMIN_TOKEN 写入 .env（openssl 随机，值未落盘文档）→ 重启 PM2（进程 60 次重启后稳定在线）→ 冒烟 | ✅ 全绿：health 200；/api/knowledge/latest 返回种子 v2026-08-22.1；/admin/admin.html 200；管理接口无 Token 403（fail-closed）；日志无 error；备份 src.bak.20260823040411-knowledge |
| 2026-08-23 | 知识图谱部署补充说明 | ⚠️ 旧迁移 003/004/005/008/009/011/012/013/014/017 仍因 owner 失败（既有问题，不影响新表）；**root 密码已暴露于一次聊天记录，建议尽快改密**；ADMIN_TOKEN 值在服务器 .env，查看方式：`ssh root@49.232.203.85 "grep '^ADMIN_TOKEN=' /opt/xinghuanhai/server/.env"` |
| 2026-08-23 | Agent 对话成本日志（AI 算账）：上传 agentService.ts（意图/工具链/token/耗时记录，try/finally 兜底客户端断开）+ 迁移 023 建 agent_conversation_logs 表（CHECK 约束 + 双索引）→ 重启 PM2 | ✅ 冒烟全绿：health 200；表结构验证（10 列 + 索引 + CHECK）；日志静默失败不阻塞对话；用户消息正文不落库 |
| 2026-08-23 | 修复喂养建议 500（缺表）：①发现迁移 012/013 原版外键引用 `pets(id)`（表不存在），生产实际为 `pet_profiles(id)`（text）——已修正迁移脚本为对齐生产 schema；②用 postgres 超级用户执行建表 `pet_feeding_records` / `pet_chronic_records` + 索引，owner 移交 xinghuanhai；③重启 PM2 | ✅ 冒烟全绿：GET/POST feeding-records 200、GET chronic 200、外键指向 pet_profiles/users 正确、应用用户可查询；测试写入数据已清理；服务健康 |
| 2026-08-23 | ⚠️ 遗留（未处理，非本次报障范围）：迁移 014（suggestion_records）、017（invite_referrals）表名与生产实际命名不一致（生产已有 hospital_referrals/invite_codes/referral_records），若对应功能 500 需单独对齐 | 待确认 |
| 2026-08-23 | 监控备份体系补齐：①星河宠记库每日备份（backup-xinghuanhai.sh，3点 cron，此前 P0 缺口：只有 qinglv 库有备份）②异地备份到本地（XHH-Backup-Pull 计划任务 7 点拉取到 E:\Backups，checksum 一致）③DeepSeek 余额告警（9 点，<¥10，当前 ¥73.21）④支付对账（9:30，异常告警，实测全零）⑤运维脚本入库 05-部署配置/monitor/ | ✅ 全部实测通过；**恢复演练**：备份还原临时库成功（56 表 + users16/pet_profiles15/agent_memories3/knowledge_graphs1），临时库已清理 |
| 2026-08-23 | **修复生产 429（点击慢性病等多处报错，与缺表 500 是两个问题）**：根因=nginx 反代未开 trust proxy（req.ip 恒 127.0.0.1）+ 全局限流跑在认证前（userId 恒空）→ 所有用户共享一个 60/分钟桶，突发即集体 429。修复：`app.set('trust proxy',1)` + 新增 `resolveUserId` 中间件（限流前解析 JWT、不拒绝请求）→ 按真实用户独立限流。验证：用户A/用户B 各突发 60 次均 0 个 429（PASS） | ✅ 已部署（d1d12e1），847/847 测试通过 |
| 2026-08-23 | 知识图谱/监控配置写入项目记忆（AGENTS.md） | ✅ 已更新 |
| 2026-08-23 | 修复宠物档案切换宠物 429（限流误伤正常操作）：全局限流 60→120 次/分钟（切换宠物时档案页/时光页多模块并发加载约 6-8 请求/次，60 次/分钟过严触发误伤）→ 上传 rateLimit.ts → 重启 PM2 | ✅ 冒烟：130 请求 = 120×200 + 10×429（新阈值精确生效）；服务端 52 文件 848 测试全绿（顺带修复 knowledge.test.ts 动态版本断言 bug） |
| 2026-08-23 | **兑换码系统上线 + 不忘 VIP 发放**：①迁移 024 建 redeem_codes 表；②`POST /api/redeem` 用户兑换发/续会员（active 顺延，CAS 防重复兑换）；③管理端 `POST/GET /api/admin/redeem-codes`（ADMIN_TOKEN，adminAuth 抽为共享中间件）；④小程序会员页新增兑换码输入入口（e1a207e，待微信发版）；⑤**走完整流程给不忘（dfae8e5b…可乐/布丁主人）发放 100 年会员**：生成码 XHH-MKXK-4FNQ-R3HL → 签 JWT → 兑换成功（expires_at 2126-07-30，memberships 表验证 tier=member/active） | ✅ 已部署（d1f8c18），855/855 测试通过 |
| 2026-08-23 | ⚠️ **管理后台访问约定**：**必须用域名 `https://api.xinghuanhai.com/admin`**，**禁用 IP `https://49.232.203.85/admin`**——共用服务器上 qxl（爱需说出口）是 443 默认站点，IP 直连会命中它并返回"接口不存在"；且证书按域名签发，IP 不匹配。管理页改 JS/CSS 后记得递增 `admin.html` 里的 `?v=` 版本号强制刷新缓存（本次 v=3） | ✅ 已修复 /admin/ 404（0489247）+ 缓存版本号（911a44c） |
