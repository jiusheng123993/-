# 研发工程效能强制规范 - 前端技术规范

> 本文件为全局规则拆分文件 2a/7。与 rule-02-development.md 配套使用。
> 前端开发任务必须遵守本文件规范。

---

## 第一部分：命名规范

### 1.1 文件命名

| 类型 | 规范 | 示例 |
|------|------|------|
| 组件文件 | PascalCase | `UserTable.vue`, `UserTable.tsx` |
| 可复用逻辑 | camelCase + 框架前缀 | `useUserTable.ts`, `usePagination.ts` |
| 工具函数 | camelCase | `formatDate.ts`, `validatePhone.ts` |
| 类型文件 | PascalCase | `UserType.ts`, `CommonType.ts` |
| 状态文件 | camelCase + Store 后缀 | `userStore.ts`, `appStore.ts` |
| 接口文件 | camelCase | `user.ts`, `order.ts` |
| 路由文件 | camelCase | `user.ts`, `order.ts` |
| 样式文件 | camelCase | `variables.scss`, `mixin.scss` |
| 常量文件 | camelCase | `enum.ts`, `index.ts` |
| 配置文件 | camelCase | `menu.ts`, `feature.ts` |
| 测试文件 | 被测文件名 + .test/.spec | `userStore.test.ts`, `formatDate.spec.ts` |

### 1.2 代码命名

| 类型 | 规范 | 示例 |
|------|------|------|
| 组件名 | PascalCase | `<UserTable />` |
| 变量/函数 | camelCase | `getUserList`, `totalCount` |
| 常量 | UPPER_SNAKE_CASE | `MAX_RETRY_COUNT`, `API_BASE_URL` |
| 类型/接口 | PascalCase | `UserInfo`, `ApiResponse` |
| 枚举 | PascalCase | `enum UserStatus` |
| 枚举值 | PascalCase | `Active`, `Inactive` |
| CSS 类名 | BEM 规范 | `.block__element--modifier` |
| 事件名 | on 前缀 + PascalCase | `onUpdateData`, `onCloseDialog` |
| Props / 参数 | camelCase | `userName`, `pageSize` |
| i18n key | 模块.页面.字段 | `user.list.title`, `order.detail.status` |

### 1.3 禁止事项

- 禁止使用拼音命名
- 禁止使用单字母变量（循环变量 i/j/k 除外）
- 禁止使用无意义命名（如 `data1`, `temp`, `foo`, `info`）
- 禁止在命名中使用缩写（通用缩写除外：URL, API, ID, DTO, VO）
- 禁止使用与保留字冲突的命名

---

## 第二部分：TypeScript 规范

### 2.1 基本规则

1. **禁止使用 `any`**：所有变量、参数、返回值必须有明确类型。如确实无法确定类型，使用 `unknown` 并做类型守卫
2. **接口类型必须定义**：每个外部接口（API / SDK / 第三方库）必须定义请求参数类型和响应数据类型
3. **类型文件独立管理**：类型定义统一放在 `types/` 目录，禁止在组件内定义全局类型
4. **使用 interface 优先**：对象类型优先使用 `interface`，联合类型和工具类型使用 `type`
5. **枚举必须显式赋值**：禁止隐式枚举值
6. **组件 Props/参数 必须类型化**：使用泛型方式定义，禁止运行时推导
7. **禁止 `@ts-ignore` / `@ts-expect-error`**：必须正确处理类型问题，不允许绕过类型检查
8. **开启 strict 模式**：tsconfig.json 必须设置 `"strict": true`
9. **禁止隐式类型推导**：函数参数和返回值必须显式声明类型
10. **环境变量必须类型声明**：通过 `ImportMetaEnv` 或 `process.env` 接口声明类型

### 2.2 类型安全实践

1. 使用类型守卫收窄类型，禁止强制类型断言（`as`），除非确有必要且添加注释说明
2. 外部数据必须经过运行时类型校验（使用 zod / joi / 自定义校验函数），不信任任何外部输入
3. 使用泛型约束减少重复类型定义
4. 使用 `satisfies` 操作符确保类型满足约束而不丢失推断
5. 区分业务类型与传输类型（DTO vs Domain Model），禁止混用

---

## 第三部分：组件规范

### 3.1 组件结构顺序

```
1. 类型导入
2. 第三方库导入
3. 组件导入
4. 可复用逻辑导入
5. 接口/状态 导入
6. 工具函数导入
7. Props / 参数 定义
8. 响应式数据
9. 计算属性 / 派生状态
10. 侦听器 / 副作用
11. 生命周期钩子
12. 方法 / 事件处理
13. 暴露的公共接口
```

### 3.2 组件规则

1. 必须使用组合式 / 函数式 API，禁止选项式 / 类式 API
2. 必须使用局部样式（scoped / CSS Modules / CSS-in-JS），防止样式污染
3. 组件 Props / 参数 必须定义默认值
4. 组件超过 200 行必须考虑拆分
5. 模板 / JSX 中禁止复杂表达式，提取为计算属性或方法
6. 禁止使用 `v-html` / `dangerouslySetInnerHTML`（除非经过安全处理并记录评审记录）
7. 列表渲染必须使用唯一 key，禁止用 index 作为 key
8. 组件必须有明确的 name / displayName，便于调试和缓存
9. 事件处理函数必须使用稳定引用，避免不必要的子组件重渲染
10. 组件销毁时必须清理：定时器、事件监听、WebSocket 连接、第三方实例

### 3.3 组件设计原则

1. **单一职责**：一个组件只做一件事，职责不清晰时必须拆分
2. **数据向下，事件向上**：禁止通过 Props 传递回调修改父组件状态
3. **智能/展示分离**：业务逻辑组件（Smart）与纯展示组件（Dumb）分离
4. **插槽 / renderProps 优先**：可定制部分优先使用插槽机制，而非 Props 覆盖
5. **受控/非受控明确**：组件要么完全受控，要么完全非受控，禁止混合模式

---

## 第四部分：接口层规范

### 4.1 请求封装

所有 HTTP 请求必须通过统一封装层调用，封装层必须包含：

1. **请求拦截器**：自动添加认证信息、处理请求参数、请求签名
2. **响应拦截器**：统一错误处理、认证过期自动刷新、错误码映射
3. **请求重试**：网络异常自动重试（最多 3 次，指数退避）
4. **请求取消**：页面切换自动取消未完成请求
5. **请求防重**：相同请求在 pending 状态时自动去重
6. **超时控制**：默认 15 秒，大文件上传等场景可单独配置
7. **禁止直接使用原始 HTTP 客户端**：必须通过封装层调用

### 4.2 接口定义规则

1. 每个接口函数必须有完整的类型定义（参数类型 + 返回类型）
2. 接口函数按业务模块组织
3. 禁止在组件中直接调用 HTTP 客户端，必须通过接口层
4. 禁止在接口函数中处理 UI 逻辑（如弹窗提示）
5. RESTful 风格：GET 查询、POST 新增、PUT 修改、DELETE 删除
6. 涉及敏感操作的接口（删除、修改）必须有二次确认机制
7. 分页接口必须支持防抖，避免快速翻页导致请求堆积
8. 文件上传接口必须限制文件类型和大小，前端做预校验
9. 接口返回数据禁止直接信任，关键数据必须做运行时校验

---

## 第五部分：状态管理规范

1. 使用框架推荐的状态管理方案（Pinia / Zustand / NgRx 等）
2. 状态按业务模块拆分
3. Store 命名格式：`useXxxStore` 或 `useXxx` + 框架后缀
4. 必须使用组合式 / 函数式风格，禁止选项式风格
5. 异步操作放在 Store 的 action 中
6. 组件中解构 Store 数据必须保持响应性（使用框架推荐方式）
7. 跨模块访问 Store 必须通过公共接口，禁止直接修改其他 Store 的内部状态
8. Store 必须提供重置能力，支持场景还原
9. 持久化数据必须加密存储，禁止明文存储敏感信息
10. Store 中禁止存放临时 UI 状态（如弹窗开关），临时状态留在组件内

---

## 第六部分：路由规范

1. 路由按业务模块拆分
2. 每个路由必须有 `name` 和 `meta` 字段
3. `meta` 必须包含：`title`（页面标题）、`icon`（菜单图标）、`localeKey`（国际化 key）
4. 需要权限的路由必须配置权限标识
5. 页面组件必须使用路由懒加载
6. 嵌套路由最多 3 层
7. 路由 name 必须全局唯一，格式为 `模块:页面`（如 `user:list`, `user:detail`）
8. 路由守卫必须处理：权限校验、认证有效性、页面标题设置、进度条
9. 404 路由必须最后注册，防止拦截正常路由
10. 路由切换必须取消未完成的接口请求，防止数据错乱

---

## 第七部分：样式规范

1. 全局样式变量（设计令牌）必须集中定义：颜色、字号、间距、阴影、圆角
2. 全局 mixin / 函数 必须集中管理
3. 组件样式必须使用局部作用域（scoped / CSS Modules / CSS-in-JS）
4. 颜色值必须使用 CSS 变量或预处理器变量，禁止硬编码颜色值
5. 间距使用 4px 倍数体系（4, 8, 12, 16, 20, 24...）
6. 字号使用设计系统定义的字号变量
7. 禁止使用 `!important`（覆盖第三方组件样式除外，但必须注释说明原因）
8. 禁止使用行内样式 `style="..."`
9. z-index 分层管理：普通元素 1-9，弹层 10-99，Modal 100-999，全局提示 1000+
10. CSS 类名使用 BEM 规范：`.block__element--modifier`
11. 响应式断点必须统一：sm / md / lg / xl / xxl
12. 主题系统通过 CSS 变量实现，支持运行时切换，禁止编译时锁定主题

---

## 第八部分：错误处理规范

1. 接口请求必须在拦截器中统一处理错误码
2. 业务错误在组件/Store 中处理，展示用户友好提示
3. 禁止空 `catch` 块，至少记录错误日志
4. 异步操作必须处理 loading 状态
5. 网络异常必须有兜底 UI（如错误页面、重试按钮）
6. 表单验证错误必须定位到具体字段并提示
7. 错误信息必须国际化，禁止硬编码错误提示
8. 未捕获异常必须有全局兜底（框架 errorHandler + 全局 error 事件）
9. 错误日志必须上报到监控系统，包含：错误堆栈、用户信息、页面路径、时间戳
10. 区分错误等级：Error（必须修复）、Warn（需要关注）、Info（记录备查）

---

## 第九部分：性能规范

1. 路由必须懒加载
2. 大列表（超过 100 条）必须使用虚拟滚动
3. 图片必须使用懒加载
4. 组件库必须按需引入，禁止全量引入
5. 频繁操作必须做防抖/节流
6. 派生计算禁止有副作用
7. 组件销毁时必须清理定时器、事件监听等
8. 避免不必要的响应式数据（纯常量使用 `const` 声明）
9. 首屏加载时间（LCP）必须 < 2.5 秒
10. 页面交互响应时间（FID/INP）必须 < 100 毫秒
11. 累计布局偏移（CLS）必须 < 0.1
12. 静态资源必须开启 gzip/brotli 压缩
13. 大型第三方库考虑使用 CDN + externals 抽离
14. 长任务（> 50ms）必须拆分为微任务，避免阻塞主线程
15. Web Vitals 指标必须接入监控，异常时自动告警

---

## 第十部分：可维护性规范

### 10.1 SOLID 原则

1. **单一职责（SRP）**：每个函数/类/模块只做一件事，函数超过 50 行必须拆分
2. **开放封闭（OCP）**：通过配置和扩展点实现新功能，而非修改已有代码
3. **里氏替换（LSP）**：子类型必须能替换其基类型，组件 Props 继承必须兼容
4. **接口隔离（ISP）**：Props 定义必须精简，禁止传递无用 Props
5. **依赖倒置（DIP）**：依赖抽象而非具体实现，接口层通过类型定义而非实现

### 10.2 代码复杂度控制

1. 单个函数圈复杂度不超过 10
2. 嵌套层级不超过 3 层（if/for/while）
3. 函数参数不超过 4 个，超过则使用对象参数
4. 单个组件文件不超过 300 行
5. 单个文件不超过 500 行
6. 回调地狱必须使用 async/await 消除

### 10.3 设计模式

1. **策略模式**：验证规则、权限校验策略使用策略模式，便于扩展
2. **工厂模式**：复杂对象创建使用工厂函数，封装创建逻辑
3. **观察者模式**：跨组件通信使用事件机制或状态管理，禁止 props 链式传递
4. **适配器模式**：第三方数据通过适配器层转换，隔离外部依赖
5. **装饰器模式**：通用功能（日志、权限、缓存）通过高阶函数/组合式函数实现

### 10.4 依赖注入与解耦

1. 接口调用通过可复用逻辑封装，组件不直接依赖接口实现
2. 第三方库调用必须封装适配层，禁止在业务代码中直接使用第三方 API
3. 配置项通过依赖注入提供，禁止硬编码
4. 模块间通过接口通信，禁止直接引用实现细节

### 10.5 版本兼容

1. 接口变更必须做版本兼容（v1/v2），旧版本至少保留 3 个月
2. 数据结构变更必须提供迁移函数
3. 本地存储数据格式变更必须提供升级策略
4. 废弃 API 必须标注 `@deprecated` 并注明替代方案和移除时间

---

## 第十一部分：可扩展性规范

### 11.1 国际化（i18n）

1. 所有用户可见文本必须使用 i18n，禁止硬编码
2. 语言包按模块组织，支持按需加载
3. 日期/时间/数字格式必须国际化
4. 布局必须支持 RTL（从右到左）语言
5. 默认语言：zh-CN，必须支持 en-US
6. i18n key 命名：`模块.页面.字段`

### 11.2 主题系统

1. 主题通过 CSS 变量实现，支持运行时切换
2. 必须支持：亮色主题、暗色主题
3. 主题变量必须包含：主色、背景色、文字色、边框色、阴影、圆角
4. UI 组件库主题必须与自定义主题变量同步
5. 主题切换必须持久化到用户偏好

### 11.3 功能开关（Feature Flag）

1. 新功能必须通过功能开关控制上线
2. 功能开关必须集中配置管理
3. 功能开关必须支持：全局开关、用户级开关、灰度百分比
4. 功能开关必须有过期时间，稳定运行 2 周后移除开关

### 11.4 插件化架构

1. 业务模块必须支持独立注册/卸载
2. 菜单、路由、权限通过配置注册，而非硬编码
3. 第三方集成通过适配器模式接入，便于替换
4. 扩展点必须文档化
