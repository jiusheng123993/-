# 研发工程效能强制规范 - 安全红线与开发流程

> 本文件为全局规则拆分文件 2/7。与其他 6 个规则文件共同构成完整规则体系。
> Agent 必须读取全部 7 个规则文件。
>
> **规则体系总览**：
> 1. rule-01-session-start.md（入口，必读）
> 2. rule-02-development.md（安全红线 + 开发流程，编码前必读）
> 3. rule-02a-frontend-spec.md（前端技术规范，编码时参考）
> 4. rule-02b-backend-security.md（后端安全审查，后端项目必读）
> 5. rule-03-codegraph.md（分析工具，代码操作前必读）
> 6. rule-04-testing.md（测试验证，自测阶段必读）
> 7. rule-05-git.md（版本管理，提交前必读）
> 8. rule-06-delivery.md（交付审计，完成时必读）
> 9. rule-07-board.md（看板自动化，贯穿全程）

---

## 第一部分：优先级体系与适用范围

### 1.1 适用范围

1.1.1 若本规则写入 Trae 全局规则，则适用于所有工作区、所有项目、所有后续 Agent。

1.1.2 若本规则写入项目规则，则仅适用于当前项目。

1.1.3 项目规则与全局规则冲突时，优先级为：用户最新明确指令 > 项目规则 > 全局规则 > Agent 默认习惯。

### 1.2 workspacePath 规则

1.2.1 所有 local-project-memory MCP 调用中的 workspacePath 必须使用当前 Trae 工作区根目录，或用户明确指定的项目根目录。

1.2.2 workspacePath 禁止使用单个文件路径、临时目录、父级汇总目录、系统目录、插件目录、缓存目录或无关目录。

1.2.3 如果当前打开的是单个文件，但 Trae 已存在工作区根目录，仍必须使用工作区根目录作为 workspacePath。

1.2.4 如果无法确定工作区根目录，必须先根据当前环境、终端 cwd、项目配置文件和用户打开路径推断最合理项目根目录，并在最终说明中标明该假设。

### 1.3 规则冲突裁决流程

1.3.1 当多条规则对同一场景给出不同指令时，按以下顺序裁决：
1. 安全红线优先于所有其他规则
2. 用户最新明确指令优先于规则
3. 具体规则优先于通用原则
4. 新版规则优先于旧版规则

1.3.2 裁决结果必须写入项目记忆，标注冲突规则和裁决依据，避免后续 Agent 重复遇到相同冲突。

---

## 第二部分：安全红线

### 2.1 禁止暴露的内容

2.1.1 任何项目中都禁止写入、打印、提交、暴露以下内容：
1. 密钥
2. Token
3. 密码
4. 数据库连接串
5. 私有证书
6. API Key
7. OAuth Secret
8. 用户隐私
9. 企业私有数据
10. 生产环境配置
11. 云服务凭据
12. 模型供应商密钥
13. 支付密钥
14. Webhook Secret

### 2.2 脱敏规则

2.2.1 日志必须脱敏。错误信息不得向前端或用户暴露内部堆栈、密钥、数据库结构、服务器路径、敏感配置。

2.2.2 如必须记录相关配置存在，只能写脱敏形式，例如 sk-****3456。

2.2.3 项目记忆中不得写入用户隐私、企业私有数据、生产环境敏感配置。

2.2.4 错误日志写入记忆前必须脱敏。

2.2.5 涉及权限、安全、租户、支付、订单、数据迁移、生产部署的记忆必须标记高 importance。

### 2.3 数据隔离与权限规则

2.3.1 如果项目涉及用户、团队、组织、企业、租户、角色、权限、订单、资金、私有数据，则必须默认启用隔离思维。

2.3.2 必须检查：
1. 是否绑定用户身份
2. 是否绑定组织或租户身份
3. 是否存在越权访问
4. 是否存在横向越权
5. 是否存在纵向越权
6. 是否存在跨租户查询
7. 是否存在跨租户缓存污染
8. 是否存在跨用户数据泄露
9. 是否存在日志泄露
10. 是否存在前端隐藏但后端未校验的问题

2.3.3 禁止只依赖前端权限控制。禁止只隐藏按钮而不做后端校验。禁止在未确认身份上下文的情况下执行敏感操作。禁止跨租户复用缓存 Key，除非明确加入隔离维度。

2.3.4 涉及鉴权、权限、租户、支付、订单、用户数据、企业数据、模型调用、文件上传、远程命令执行时，必须优先进行安全风险判断。

### 2.4 敏感操作二次确认机制

2.4.1 以下操作必须先询问用户，不得擅自执行：
1. 删除大量文件
2. 覆盖配置文件
3. 重置 Git 分支
4. 清空数据库
5. 执行生产数据迁移
6. 修改生产环境配置
7. 强制 push
8. 删除远程分支
9. 替换核心框架
10. 替换数据库
11. 替换认证体系
12. 修改支付、订单、权限、租户、安全核心逻辑
13. 提交或推送代码
14. 安装来源不明依赖
15. 执行远程脚本
16. 执行不可逆命令

2.4.2 确认格式要求：必须记录确认内容、确认时间、用户回复，写入项目记忆。

### 2.5 自动修复安全排除清单

2.5.1 高风险关键词定义：权限、安全、租户、支付、订单、数据库结构、认证、密钥、Token、生产配置。

2.5.2 凡是修改内容命中以上高风险关键词的，即使看起来简单也必须先确认，不得自动修复。

2.5.3 自动修复执行前，必须校验修改范围是否命中安全排除清单；命中则升级为确认流程。

---

## 第三部分：开发迭代流程

### 3.1 研发流程前置强制约束

3.1.1 所有代码开发任务必须按顺序走完完整前置流程，未完成前置分析、未获得确认，禁止编写任何业务代码、配置代码、脚本代码。

3.1.2 严格遵循标准化闭环流程：需求分析 → 可行性分析 → 需求拆解 → 架构先行 → 风险排查 → 确认后编码 → 单模块独立开发 → 自检审计 → 测试覆盖 → 文档同步 → 提交推送 → 迭代下一模块。

3.1.3 禁止一次性编写全部代码。完整档严格单模块串行开发，标准档同模块内允许，轻量档允许同模块多文件。

3.1.4 单模块完成并得到确认后，方可进入下一模块。完整档单模块开发完成后，必须切换第三方资深安全架构师身份，批判性全盘审查所有代码，遍历排查隐性设计缺陷、逻辑漏洞、安全短板、不合理架构、性能隐患、兼容问题，逐条完整修复闭环。

3.1.5 所有功能开发完成后，必须自动补齐标准化代码注释、生成全覆盖单元测试用例，覆盖正常场景、边界场景、异常场景、高并发兜底场景。

3.1.6 每个模块开发完毕后，必须输出标准化模块文档，同步提交推送至开发分支，校验无误、评审通过后，方可合并至主干分支，文档与代码版本强制绑定归档。

### 3.2 架构继承原则

3.2.1 所有项目默认遵循"继承现有架构，优先扩展，不轻易重写"的原则。

3.2.2 面对新需求时，必须按以下优先级选择实现方式：
1. 使用已有配置项实现
2. 使用已有扩展点实现
3. 新增适配器 Adapter
4. 新增策略 Strategy
5. 新增 Provider
6. 新增 Handler
7. 新增 Resolver
8. 新增 Service
9. 新增 Hook / Middleware / Plugin
10. 在现有模块中追加兼容逻辑
11. 小范围调整接口并保留兼容层
12. 修改核心流程
13. 重构架构

3.2.3 只有前面的方式无法满足需求，并且已经完成影响分析、测试方案和回滚方案后，才允许考虑修改核心流程或重构架构。

3.2.4 禁止为了实现一个局部需求直接推翻整体架构。禁止因为短期方便绕过已有模块边界。禁止重复实现已有能力。禁止把不同层级的职责混写在一个文件或函数中。

### 3.3 模块边界规则（七层架构）

3.3.1 所有项目都必须尽量保持模块职责清晰。智能体必须根据项目类型自动映射以下 7 层边界：

**1. 表现层 / UI 层**
   - 职责：页面展示、组件渲染、用户交互、表单输入、状态展示、基础校验、路由跳转
   - 禁止：直接写复杂业务规则、直接访问数据库、直接拼接底层接口、直接处理权限核心逻辑、直接写大段数据转换和持久化逻辑

**2. 接口层 / Controller / Route 层**
   - 职责：接收请求、参数校验、身份校验、调用业务服务、返回统一响应、处理接口错误
   - 禁止：直接写复杂业务编排、直接写数据库底层细节、直接绕过 Service、直接泄露内部错误栈或敏感信息

**3. 业务服务层 / Service 层**
   - 职责：承载核心业务规则、编排多个依赖模块、处理业务状态、处理业务异常、保证业务一致性
   - 禁止：直接依赖具体 UI、直接依赖具体第三方 SDK 的散乱调用、直接拼接 SQL、直接绕过权限/审计/事务或隔离规则

**4. 数据访问层 / Repository / DAO 层**
   - 职责：封装数据库读写、封装查询条件、封装事务、封装数据模型映射
   - 禁止：上层模块直接散落数据库访问逻辑、绕过租户/权限或数据隔离条件、把业务规则大量写入底层查询中

**5. 第三方集成层 / Adapter / Provider 层**
   - 职责：封装外部平台、SDK、API、模型、支付、消息、存储、搜索、向量库等外部依赖
   - 禁止：业务层到处直接调用第三方 SDK、把供应商私有结构泄露到核心业务层、把密钥/Token/Secret 写入代码

**6. 工具层 / Utils / Common 层**
   - 职责：纯函数工具、格式转换、通用校验、无业务状态的辅助逻辑
   - 禁止：写入具体业务流程、访问数据库、访问外部服务、读取敏感配置、包含隐藏副作用

**7. 配置层 / Config 层**
   - 职责：统一管理配置、提供默认值、校验配置合法性、隔离环境差异
   - 禁止：配置散落在业务代码中、硬编码密钥、硬编码环境地址、硬编码生产参数

### 3.4 扩展点规则

3.4.1 常见扩展点包括：
- Adapter：用于接入不同平台、渠道、数据源、协议、第三方系统
- Provider：用于替换不同供应商、模型、存储、搜索、支付、消息服务
- Strategy：用于不同业务策略、规则、排序、匹配、路由、降级方案
- Resolver：用于解析身份、租户、配置、上下文、路由、权限等
- Handler：用于处理事件、任务、回调、异常、兜底逻辑
- Middleware：用于统一处理鉴权、日志、错误、限流、审计、上下文注入
- Plugin：用于可插拔能力扩展
- Hook：用于生命周期扩展
- Repository：用于数据访问隔离
- Service：用于业务能力封装

3.4.2 新增功能时，必须优先判断是否属于以上扩展点。如果属于扩展点，应新增实现，而不是修改核心主流程。

### 3.5 接口契约规则

3.5.1 所有对外接口、内部服务接口、公共函数、组件 Props、事件、配置、数据库模型、消息结构、任务结构，都视为契约。

3.5.2 契约包括：入参结构、返回结构、错误结构、状态码、错误码、枚举值、事件名称、配置项名称、字段语义、默认行为、权限要求、数据隔离要求、重试行为、超时行为、降级行为。

3.5.3 禁止随意破坏契约。

3.5.4 兼容变更（通常允许）：新增可选字段、新增可选配置、新增枚举值并保留默认处理、新增 Adapter/Provider/Handler 实现、新增非破坏性返回字段、增加更严格但不破坏旧流程的内部校验。

3.5.5 风险变更（必须先分析和说明）：修改字段含义、修改默认行为、修改错误码、修改权限判断、修改缓存策略、修改重试策略、修改超时时间、修改数据查询范围、修改排序规则、修改状态流转。

3.5.6 高危变更（未经影响分析、迁移方案、兼容方案、测试方案、回滚方案，不得直接执行）：删除必填字段、删除已有返回字段、修改主键语义、修改租户隔离语义、修改用户身份语义、修改权限模型、修改历史 API 返回结构、移除已有扩展点、重写核心业务流程、数据库结构大改、批量删除文件、替换核心框架、替换认证体系、替换数据存储方案。

### 3.6 全局核心原则

3.6.1 先识别项目，再分析代码。
3.6.2 先理解架构，再修改功能。
3.6.3 先确认模块边界，再进入编码。
3.6.4 先分析影响范围，再执行改动。
3.6.5 先复用已有扩展点，再考虑新增实现。
3.6.6 先保持兼容，再增加能力。
3.6.7 先补测试和验证，再宣称完成。
3.6.8 先保护用户已有改动，再修改文件。
3.6.9 先输出风险和限制，再处理高风险变更。
3.6.10 后续智能体必须能够基于现有规则、代码和记录继续接手。
3.6.11 任何智能体不得在未完成项目识别、未完成结构分析、未完成影响范围判断、未明确测试和验证方式的情况下，直接修改业务代码。

---

## 第四部分：技术选型与工程化基础

### 4.1 技术选型原则

4.1.1 必须使用 TypeScript，开启 strict 模式
4.1.2 必须使用组件化框架（Vue / React / Angular 等）
4.1.3 必须使用现代构建工具（Vite / Webpack 5+ / Turbopack 等）
4.1.4 必须集成代码检查工具（ESLint + Prettier + Stylelint 或等价工具）
4.1.5 必须集成提交规范工具（Husky + lint-staged + commitlint 或等价工具）
4.1.6 必须集成单元测试框架（Vitest / Jest 等）
4.1.7 必须集成 E2E 测试框架（Playwright / Cypress 等）
4.1.8 必须集成国际化方案（vue-i18n / react-i18next / 等）
4.1.9 必须集成依赖安全审计（npm audit / pnpm audit / 等）

### 4.2 依赖管理规则

4.2.1 禁止引入未经评估的第三方库，新增依赖必须提交团队评审
4.2.2 每个新增依赖必须评估：维护状态、包体积、安全记录、许可证兼容性
4.2.3 锁文件必须提交到版本控制
4.2.4 每月执行一次依赖安全审计，高危漏洞必须在 48 小时内修复，中危漏洞必须在 1 周内修复
4.2.5 禁止使用已停止维护的库（近 6 个月无更新需评估）
4.2.6 优先选择社区活跃度高、TypeScript 支持好的库
4.2.7 禁止使用已知漏洞的依赖版本
4.2.8 第三方脚本必须通过 SRI（Subresource Integrity）校验

---

## 第五部分：项目架构与目录规范

### 5.1 目录组织原则

5.1.1 按业务模块组织代码，每个模块独立一个文件夹
5.1.2 每个业务模块必须包含完整的垂直切片：页面、路由、接口、状态、类型、语言包
5.1.3 公共代码必须与业务代码分离
5.1.4 配置代码必须与逻辑代码分离
5.1.5 常量必须集中管理，禁止散落在业务代码中

### 5.2 必需目录结构

```
src/
├── api/                    # API 接口层（统一封装 + 按模块组织）
├── assets/                 # 静态资源（样式、图片、图标、字体）
├── components/             # 公共组件
│   ├── common/             # 通用基础组件
│   └── business/           # 业务公共组件
├── composables/            # 可复用逻辑（hooks / composables / utils）
├── config/                 # 应用配置（菜单、功能开关等）
├── constants/              # 常量与枚举
├── directives/             # 自定义指令（如权限指令）
├── i18n/                   # 国际化（入口 + 语言包）
├── layouts/                # 页面布局组件
├── logger/                 # 日志模块
├── router/                 # 路由配置（按模块拆分 + 守卫）
├── stores/                 # 状态管理（按模块拆分）
├── types/                  # 类型定义（通用类型 + 按模块拆分）
├── utils/                  # 工具函数（缓存、加密、验证、格式化等）
├── views/                  # 页面视图
│   ├── error/              # 错误页面（403/404/500）
│   └── modules/            # 按业务模块组织页面
└── [入口文件]              # main.ts / App.vue / index.ts 等
```

### 5.3 模块隔离规则

5.3.1 禁止跨模块直接引用另一个模块的内部组件，必须提取到公共组件目录中共享
5.3.2 禁止跨模块直接引用另一个模块的内部状态，必须通过公共接口访问
5.3.3 禁止跨模块直接引用另一个模块的路由定义
5.3.4 模块间通信必须通过：公共状态管理、事件机制、公共接口

---

## 第六部分：工程反馈循环与需求对齐

### 6.1 Diagnose 规则

6.1.1 处理 Bug、异常、性能问题时，必须先构建可运行、可重复、尽量确定的反馈循环。优先级为：失败测试 → HTTP/curl 脚本 → CLI 复现 → 浏览器自动化 → captured trace 回放 → 最小 harness → fuzz/property loop → bisect/differential loop → 人工辅助复现。

6.1.2 未建立反馈循环前，不得把猜测性修改当作最终修复。若确实无法复现，必须说明已尝试方式、缺失信息、需要用户提供的日志/录屏/HAR/环境访问或授权的临时 instrumentation。

6.1.3 复现后必须提出 3-5 个可证伪假设，每个假设写明预测结果。优先验证最可能、最便宜、风险最低的假设；一次只改变一个变量。

6.1.4 调试 instrumentation 必须最小化、可清理、不得泄露敏感信息。临时日志应使用唯一前缀，修复完成后必须删除；性能问题优先建立基准和 profiler/计时信号，不得盲目加日志。

6.1.5 修复 Bug 时，若存在合适测试 seam，必须先把复现最小化为失败的回归测试，再修复并验证通过；若不存在合适 seam，必须记录"当前架构缺少可测试 seam"的风险并建议后续架构改进。

### 6.2 TDD 规则

6.2.1 复杂功能和核心逻辑优先按垂直切片推进。每一轮只写一个描述可观察行为的测试或验证脚本，只实现通过该测试所需的最小代码，通过后再进入下一行为，全部通过后再重构。

6.2.2 禁止水平切片式开发：不得一次性写完所有测试、所有接口或所有实现后再统一验证。测试必须验证公共接口和用户可观察行为，避免耦合私有方法、内部结构或临时实现细节。

### 6.3 轻量 grilling 规则

6.3.1 复杂需求编码前必须先整理项目术语、领域对象、边界条件、非目标、验收行为、兼容性要求、风险假设。能通过代码、记忆、文档确认的问题自行确认；只有高风险歧义才询问用户。

6.3.2 领域语言规则：稳定术语、业务边界、架构决策和反复出现的表达，优先沉淀到 local-project-memory。仅在项目已有 CONTEXT.md、ADR 或用户明确要求时，才更新对应文档；新增文档必须遵守文档管理规则。

### 6.4 Zoom-out 规则

6.4.1 解释或修改陌生代码时，必须先说明该模块在系统中的位置、调用入口、下游依赖、数据/状态流、契约、测试入口和风险点，再给出局部修改建议。

### 6.5 架构深度规则

6.5.1 架构审查和重构建议必须使用 Module、Interface、Implementation、Depth、Seam、Adapter、Leverage、Locality 等统一词汇，优先寻找把复杂度收敛到深模块的机会。

6.5.2 架构建议必须可执行、可分阶段、可验证、可回滚。不得只输出抽象原则；必须说明涉及文件/模块、当前摩擦、建议改法、测试收益、兼容风险和优先级。

6.5.3 大任务拆分必须按用户可观察行为或可交付能力纵向拆分，每个切片包含必要的接口、实现、测试和验证；禁止仅按 UI、API、数据库、测试等水平层次拆分导致长期不可运行。

6.5.4 Handoff 增强：长任务交接必须记录当前反馈循环、已验证假设、失败假设、剩余假设、测试 seam、临时 instrumentation 清理状态、架构风险和下一步最小可执行动作。

---

## 第七部分：全局核心原则（一句话原则）

7.1 无论任何项目、任何语言、任何框架、任何任务，智能体都必须以"可维护、可测试、可交接、可扩展、可回滚"为最高工程目标；优先理解现有项目，优先使用已有架构和扩展点，优先保护用户已有改动，优先完成测试验证，禁止无分析直接修改核心逻辑，禁止破坏兼容性，禁止让后续智能体无法接手。

7.2 任何智能体不得在未完成项目识别、未完成结构分析、未完成影响范围判断、未明确测试和验证方式的情况下，直接修改业务代码。

7.3 安全红线优先于所有其他规则。用户最新明确指令优先于规则。具体规则优先于通用原则。
