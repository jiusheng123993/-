# 研发工程效能强制规范 - 后端安全审查

> 本文件为全局规则拆分文件 2b/7。与 rule-02-development.md 配套使用。
> 所有包含后端接口的项目（全栈项目、后端项目、API 服务、BFF 层、微服务）必须遵守本文件规范。

---

## 第一部分：适用范围与触发条件

### 1.1 适用范围

1.1.1 本节适用于所有包含后端接口的项目（全栈项目、后端项目、API 服务、BFF 层、微服务）。

1.1.2 以下场景必须执行后端安全审查：
1. 新项目上线前
2. 新增支付、订单、权限、认证、用户数据相关接口
3. 用户明确提出"安全检查""安全审查""后端安全"
4. 每个版本迭代的 PR 合并前（作为安全门禁的一部分）
5. 接手陌生后端项目时

1.1.3 安全审查必须覆盖以下六大维度，缺一不可。

---

## 第二部分：维度一 - 输出后端安全边界表

### 2.1 安全边界表格式

2.1.1 必须按以下六个方面逐项输出安全边界表，每个方面必须回答五个问题：
- 风险是什么？
- 当前在哪里处理？
- 处理规则是什么？
- 如何验证？
- 哪些还没有验证？

2.1.2 六个方面：
1. 接口输入安全：前端参数如何校验、哪些参数影响身份/权限/金额/库存/数据归属/数据库写入
2. 登录状态：哪些接口需要登录、token 如何校验、未登录返回什么
3. 系统权限设计：角色定义、权限判断位置、横向/纵向越权防护
4. 密码规则：密码存储方式（必须哈希不可逆）、强度要求、管理员额外约束
5. 数据归属：每条数据是否绑定用户/租户、跨用户访问如何阻断
6. 注入风险：SQL 注入、命令注入、HTML/模板注入的防护位置和方式

2.1.3 安全边界表必须写入项目记忆（type=risk），标记高 importance。

2.1.4 输出语言要求：
- 默认使用专业技术语言，确保精确性
- 如果用户表示"看不懂""太专业""用大白话"，必须用通俗语言重新解释，禁止使用技术名词（如"JWT""ORM""RLS""中间件""参数化查询"等），改用日常比喻说明风险和防护方式
- 大白话版本也必须覆盖全部六个方面和五个问题，不得因简化而遗漏风险点

---

## 第三部分：维度二 - 审查接口输入安全

### 3.1 逐接口审查

3.1.1 必须逐接口审查，每个接口回答以下问题：
- 哪些参数来自前端？
- 哪些参数会影响身份、系统权限、金额、库存、数据归属或数据库写入？
- 每个关键参数在哪里校验？
- 校验失败返回什么？
- 是否有示例请求证明（测试用例或 curl 命令）？

3.1.2 关键参数定义（必须重点审查）：
- 身份类：userId、token、sessionId、phoneNumber、email
- 权限类：role、permission、scope
- 资金类：amount、price、currency、balance
- 库存类：quantity、stock、inventory
- 归属类：orderId、resourceId、tenantId
- 写入类：任何会写入数据库的字段

3.1.3 校验规则要求：
- 所有关键参数必须有白名单或正则校验，不得仅依赖类型检查
- userId/orderId 等标识符必须校验格式（正则）和归属（是否属于当前用户）
- 金额必须校验非负、精度、上限
- 枚举值必须白名单校验

---

## 第四部分：维度三 - 审查账号和密码安全规则

### 4.1 密码处理审查

4.1.1 必须审查以下五个环节的密码处理：
1. 注册：密码强度要求在哪里校验？最低长度、复杂度要求是什么？
2. 登录：是否限制失败次数？是否有账户锁定机制？
3. 修改密码：是否要求旧密码验证？新密码是否与旧密码不同？
4. 重置密码：是否有安全的验证链路（邮箱/手机验证码）？重置 token 是否一次性、有时效？
5. 创建管理员账号：是否有额外安全约束（如必须由已有管理员创建、IP 白名单）？

4.1.2 密码存储硬性要求：
- 密码必须使用哈希（不可逆）存储，禁止明文、禁止可逆加密
- 推荐 bcrypt、argon2、scrypt，禁止 MD5、SHA1、SHA256 直接哈希不加盐
- 如果项目使用 OAuth 无密码体系，必须说明并确认 JWT 密钥未硬编码

4.1.3 管理员账号额外约束：
- 管理员账号必须强制高强度密码（≥16 位或等效复杂度）
- 管理员操作必须记录审计日志
- 生产环境必须禁用开发模式管理员 token（如 dev-admin:xxx）

---

## 第五部分：维度四 - 输出系统权限设计表

### 5.1 逐接口权限设计

5.1.1 必须逐接口输出权限设计表，每个接口回答以下问题：
- 是否需要登录？
- 允许哪些角色访问？
- 是否只能操作自己的数据？
- 管理员是否可以访问？
- 权限判断在哪个文件或中间件处理？
- 无权限时返回什么（状态码和错误信息）？
- 有没有测试证明？

5.1.2 权限设计表必须覆盖所有对外暴露的接口，包括：
- REST API 路由
- WebSocket 事件
- GraphQL mutation/query
- RPC 方法
- Webhook 回调

5.1.3 权限判断位置必须明确到文件和函数名，不得只写"在中间件处理"。

---

## 第六部分：维度五 - 检查注入风险

### 6.1 四类注入风险检查

6.1.1 必须逐项检查以下四类注入风险：

**SQL 注入：**
- 是否所有数据库操作使用 ORM 或参数化查询？
- 是否存在字符串拼接 SQL 的情况？
- 是否存在动态表名/列名/排序字段来自用户输入的情况？
- 如果使用 ORM，是否使用了原生查询（raw query）？原生查询是否参数化？

**命令注入：**
- 是否使用了 exec、spawn、eval、Function 等执行用户输入？
- 文件路径是否来自用户输入？是否做了路径遍历防护？
- 是否使用了模板引擎渲染用户输入？

**HTML/模板注入（XSS）：**
- 后端是否直接返回用户输入到 HTML 响应？
- API 返回的用户内容是否标记了 Content-Type？
- 存储到数据库的用户内容在输出时是否转义？

**日志注入：**
- 日志是否直接写入用户输入？是否做了换行符清洗？
- 错误响应是否泄露堆栈、路径、数据库结构？

6.1.2 发现任何字符串拼接用户输入的情况，必须标记为风险并给出修复方案。

---

## 第七部分：维度六 - 审查是否过度防御

### 7.1 四类校验区分

7.1.1 必须区分四类校验，避免为了安全感新增重复判断：

**必须校验的业务规则（保留）：**
- 横向/纵向越权检查
- 订单状态机校验
- 资金/库存上下限
- 数据归属校验

**框架已经处理的基础校验（可简化）：**
- Express/Spring/框架自带的 JSON 解析和类型转换
- ORM 自带的参数化查询防注入
- TypeScript/Java 编译时类型检查已覆盖的简单类型判断

**可以由前端体验层处理的提示校验（保留但可降级）：**
- 字段非空提示（后端保留作为安全兜底是合理的）
- 格式提示（如手机号格式）

**永远不会触发的防御分支（死代码，必须清理）：**
- 函数体为空但声称做了校验的代码
- 参数标记为未使用（_ 前缀）但声称已处理的逻辑
- 永远返回 false/true 的条件分支

7.1.2 任何新增安全封装都必须说明解决什么真实风险，不得为了"安全感"新增重复判断或不可能成立的条件。

---

## 第八部分：安全审查交付要求

### 8.1 交付物清单

8.1.1 安全审查完成后必须输出：
1. 后端安全边界表（六大方面 × 五个问题）
2. 接口输入安全审查报告（逐接口）
3. 账号和密码安全审查报告
4. 系统权限设计表（逐接口）
5. 注入风险检查报告（四类注入 × 逐项检查）
6. 过度防御审查报告（四类区分）

8.1.2 审查结果必须写入项目记忆（type=risk），标记高 importance。

8.1.3 发现的风险必须按 P0-P5 分级，P0/P5 必须立即阻断流程。

8.1.4 安全审查报告纳入交付摘要的【剩余风险】部分。

### 8.2 风险分级标准

| 级别 | 定义 | 处理方式 |
|------|------|----------|
| P0 | 严重安全漏洞，可导致数据泄露/资金损失/系统被控 | 立即阻断，必须修复后才能继续 |
| P1 | 高风险安全问题，可能导致越权/注入/信息泄露 | 当前轮次必须修复 |
| P2 | 中风险安全问题，需要关注但非紧急 | 记录并安排修复计划 |
| P3 | 低风险安全问题，建议优化 | 记录，可延后处理 |
| P4 | 安全建议，非必须 | 记录，作为后续改进参考 |
| P5 | 过度防御/死代码 | 清理，提升代码质量 |
